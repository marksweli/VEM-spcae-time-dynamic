# 空时虚元法弹性动力学——全公式推导解析

> **论文**：Space-time virtual element method for elastodynamics: Theory, applications, and code development  
> **作者**：Bing-Bing Xu, Philipp Junker, Peter Wriggers  
>
> 本文档按照论文的推导顺序，对论文中的**全部公式**进行解释与推导，对关键公式给出严格的数学步骤，并指出每一步所用到的数学事实。公式均使用 `[ ... ]` 格式渲染。

---

## 总体流程概述

作者的研究流程如下：

1. **建立物理问题**：给出时空柱体、Hamilton 作用量
2. **混合变分公式**：引入速度 [v] 为独立变量，推导两场弱形式
3. **时间迎风稳定化**：对测试函数加时间导数扰动，保证数值稳定性
4. **VEM 空间离散**：定义投影算子 [ \Pi_\nabla^{k,E} ] 和 [ \Pi_{k,E}^0 ]，构造离散双线性形式
5. **张量积时间离散**：利用 Lagrange 插值在时间方向离散，通过 Kronecker 积高效组装刚度矩阵
6. **FEM 参照**：给出有限元版本作为对比
7. **数值算例**：验证精度、稳定性和效率

---

## 一、物理问题与时空柱体（§2.1）

### 1.1 时空柱体的定义

设空间域 [ \Omega \subset \mathbb{R}^n ]（[ n = 1,2,3 ]），时间区间 [ I = (0,T) ]。定义**时空柱体**：

[ Q \coloneqq \Omega \times I \tag{1} ]

**物理含义**：把时间 [ t ] 看作与空间坐标完全对等的第 [ (n+1) ] 维坐标，从而将动力学问题转化为定义在 [ (n+1) ] 维时空域 [ Q ] 上的边值问题——这是整篇论文"空时方法"的出发点。

---

### 1.2 Hamilton 作用量泛函 [ H ]（公式 (2)–(3)）

Hamilton 动力学的出发点是**作用量泛函**。设线密度（动量密度向量）

[ \boldsymbol{\rho} \coloneqq \rho \nabla_t \boldsymbol{u} ]

其中 [ \rho ] 为质量密度，[ \nabla_t = \frac{\mathrm{d}}{\mathrm{d}t} ] 为时间导数，[ \boldsymbol{u} ] 为位移场。沿路径 [ [\boldsymbol{u}_0, \boldsymbol{u}_T] ] 对 [ \boldsymbol{\rho} ] 积分得到作用量：

[ H \coloneqq \int_{\Omega}\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T} \boldsymbol{\rho} \cdot \mathrm{d}\boldsymbol{u} \, \mathrm{d}V \tag{2a} ]

**推导步骤（(2) 的展开）**：

将路径积分化为时间积分。由于 [ \mathrm{d}\boldsymbol{u} = \nabla_t \boldsymbol{u} \, \mathrm{d}t ]，代入得

[ H = \int_{\Omega} \int_0^T \rho \nabla_t \boldsymbol{u} \cdot \nabla_t \boldsymbol{u} \, \mathrm{d}t \, \mathrm{d}V = \int_I \int_\Omega \rho \|\nabla_t \boldsymbol{u}\|^2 \, \mathrm{d}V \, \mathrm{d}t = \int_I 2\mathcal{K} \, \mathrm{d}t \tag{2} ]

其中**动能**定义为

[ \mathcal{K} \coloneqq \int_\Omega \frac{1}{2} \rho \|\nabla_t \boldsymbol{u}\|^2 \, \mathrm{d}V \tag{3} ]

**用到的数学事实**：
- **链式法则**：将沿位移路径的线积分转化为时间参数积分
- **Fubini 定理**：交换积分顺序 [ \int_\Omega \int_0^T (\cdots) \, \mathrm{d}t \, \mathrm{d}V = \int_I \int_\Omega (\cdots) \, \mathrm{d}V \, \mathrm{d}t ]（被积函数足够正则时成立）
- 内积写法：[ \rho \nabla_t \boldsymbol{u} \cdot \nabla_t \boldsymbol{u} = \rho \|\nabla_t \boldsymbol{u}\|^2 ]

---

### 1.3 能量守恒（公式 (4)）

由**热力学第一定律（能量守恒）**，在时间区间 [ I ] 上有

[ \int_I \mathcal{K} \, \mathrm{d}t + \int_I \mathcal{G} \, \mathrm{d}t = 0 \tag{4} ]

其中 [ \mathcal{G} ] 为**总势能**（包括弹性应变能、体力势和面力势的负值）。

**物理解读**：动能与势能之和随时间的积分为零，等价于在时间区间内守恒。这是 Hamilton 原理的能量守恒形式，并非孤立系统的机械能守恒，而是推广到含外力的功能定理形式。

---

### 1.4 总势能与自由能密度（公式 (5)–(8)）

**总势能**为

[ \mathcal{G} \coloneqq \int_\Omega \psi \, \mathrm{d}V - \int_\Omega \bar{\boldsymbol{f}} \cdot \boldsymbol{u} \, \mathrm{d}V - \int_{\Gamma_\sigma} \bar{\boldsymbol{t}} \cdot \boldsymbol{u} \, \mathrm{d}\Gamma \tag{5} ]

三项分别为：弹性应变能、体力做的功（取负号化为势能形式）、边界面力做的功。

**自由能密度**（线弹性小应变假设下的 Helmholtz 自由能）：

[ \psi \coloneqq \frac{\lambda}{2}\left(\mathrm{tr}(\boldsymbol{\varepsilon})\right)^2 + \mu \, \boldsymbol{\varepsilon}:\boldsymbol{\varepsilon} = \frac{1}{2}\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} \tag{6} ]

**四阶弹性张量**（各向同性线弹性材料，Lamé 常数 [ \lambda, \mu ]）：

[ \mathbb{D}_{ijkl} = \lambda \delta_{ij}\delta_{kl} + \mu\left(\delta_{ik}\delta_{jl} + \delta_{il}\delta_{jk}\right) \tag{7} ]

**对称梯度（应变张量）**：

[ \boldsymbol{\varepsilon} = \frac{1}{2}\left(\nabla \boldsymbol{u} + \nabla \boldsymbol{u}^T\right) =: \nabla^{\mathrm{sym}}\boldsymbol{u} = \boldsymbol{\varepsilon}(\boldsymbol{u}) \tag{8} ]

**推导公式 (6) 的等价性**：  
利用公式 (7) 代入 [ \boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} ]，展开得：

[ \boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} = \varepsilon_{ij}\,\mathbb{D}_{ijkl}\,\varepsilon_{kl} = \lambda(\varepsilon_{ii})(\varepsilon_{kk}) + \mu(\varepsilon_{ij}\varepsilon_{ij} + \varepsilon_{ij}\varepsilon_{ji}) ]

因为 [ \boldsymbol{\varepsilon} ] 对称（[ \varepsilon_{ij} = \varepsilon_{ji} ]），所以 [ \varepsilon_{ij}\varepsilon_{ji} = \varepsilon_{ij}\varepsilon_{ij} ]，从而

[ \boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} = \lambda\left(\mathrm{tr}\,\boldsymbol{\varepsilon}\right)^2 + 2\mu\,\boldsymbol{\varepsilon}:\boldsymbol{\varepsilon} ]

故 [ \psi = \frac{1}{2}\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} = \frac{\lambda}{2}\left(\mathrm{tr}\,\boldsymbol{\varepsilon}\right)^2 + \mu\,\boldsymbol{\varepsilon}:\boldsymbol{\varepsilon} ]，与 (6) 一致。

**用到的数学事实**：
- Einstein 求和约定（张量缩并）
- [ \boldsymbol{\varepsilon} ] 的对称性：[ \varepsilon_{ij} = \varepsilon_{ji} ]，这来自应变张量对称化的定义 (8)
- Kronecker δ 的性质：[ \delta_{ij}\delta_{kl}\varepsilon_{ij}\varepsilon_{kl} = \left(\mathrm{tr}\,\boldsymbol{\varepsilon}\right)^2 ]

---

### 1.5 Hamilton 作用量的重构（公式 (9)）

将 (4) 与 (2) 结合，Hamilton 作用量可等价写为

[ H = \int_I \left(\mathcal{K} - \mathcal{G}\right) \mathrm{d}t \tag{9} ]

**说明**：这正是经典力学中"Lagrangian [ L = T - V ]"对时间的积分形式，[ \mathcal{K} ] 为动能，[ \mathcal{G} ] 为势能。这里以积分等于零的形式呈现（来自能量守恒方程 (4)），因而驻值条件 [ \delta H = 0 ] 等价于求方程的极值。

---

## 二、混合变分公式（§2.1 续）

### 2.1 引入速度场 [ \boldsymbol{v} ] 作为独立变量（公式 (10)–(11)）

为了构造**两场弱形式**，将速度 [ \boldsymbol{v} ] 作为独立变量（不再要求 [ \boldsymbol{v} = \nabla_t \boldsymbol{u} ] 先验成立），把动能改写为如下**Lagrange 混合形式**：

[ \mathcal{K}_{\mathrm{mix}} = \int_\Omega \rho \boldsymbol{v} \cdot \nabla_t \boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^2 \, \mathrm{d}\Omega \tag{10} ]

**验证退化性**：当约束 [ \boldsymbol{v} = \nabla_t \boldsymbol{u} ] 满足时，

[ \mathcal{K}_{\mathrm{mix}} = \int_\Omega \rho\nabla_t\boldsymbol{u}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2 \, \mathrm{d}\Omega = \int_\Omega \frac{1}{2}\rho\|\nabla_t\boldsymbol{u}\|^2 \, \mathrm{d}\Omega = \mathcal{K} ]

与原动能定义 (3) 完全一致。

**数学事实**：这是一种经典的 **Legendre 变换思想**——通过引入拉格朗日乘子（此处即 [ \boldsymbol{v} ]）将约束条件 [ \boldsymbol{v} = \nabla_t\boldsymbol{u} ] 松弛到变分中，从而构造混合形式。

---

**Hamilton 驻值原理**（两场形式）：

[ H[\boldsymbol{v}, \boldsymbol{u}, \nabla^{\mathrm{sym}}\boldsymbol{u}, \nabla_t\boldsymbol{u}] \to \mathrm{stat} \;\Leftrightarrow\; \delta H = \delta_{\boldsymbol{v}}H + \delta_{\boldsymbol{u}}H = 0 \quad \forall \delta\boldsymbol{v}, \delta\boldsymbol{u} \tag{11} ]

因 [ \delta\boldsymbol{v} ] 和 [ \delta\boldsymbol{u} ] **独立**，可分别令各自的变分为零：

[ \begin{cases} \delta_{\boldsymbol{v}}H = 0 & \forall \delta\boldsymbol{v} \\ \delta_{\boldsymbol{u}}H = 0 & \forall \delta\boldsymbol{u} \end{cases} \tag{12} ]

---

### 2.2 变分条件的展开（公式 (13)）

对 [ H = \int_I(\mathcal{K}_{\mathrm{mix}} - \mathcal{G})\,\mathrm{d}t ] 分别计算两个变分。

**对 [ \boldsymbol{v} ] 的变分 [ \delta_{\boldsymbol{v}}H ]**：  
[ \mathcal{K}_{\mathrm{mix}} ] 中与 [ \boldsymbol{v} ] 相关的项为 [ \rho\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^2 ]，对 [ \boldsymbol{v} ] 变分：

[ \delta_{\boldsymbol{v}}\mathcal{K}_{\mathrm{mix}} = \int_\Omega \rho\left(\nabla_t\boldsymbol{u} - \boldsymbol{v}\right)\cdot\delta\boldsymbol{v} \, \mathrm{d}V ]

因此：

[ \delta_{\boldsymbol{v}}H = \int_I\int_\Omega \rho\left(\nabla_t\boldsymbol{u} - \boldsymbol{v}\right)\cdot\delta\boldsymbol{v} \, \mathrm{d}V \, \mathrm{d}t = 0 \quad \forall\delta\boldsymbol{v} \tag{13a} ]

**对 [ \boldsymbol{u} ] 的变分 [ \delta_{\boldsymbol{u}}H ]**：  
[ \mathcal{K}_{\mathrm{mix}} ] 中与 [ \boldsymbol{u} ] 相关的项为 [ \rho\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} ]，对 [ \boldsymbol{u} ] 变分：

[ \delta_{\boldsymbol{u}}\mathcal{K}_{\mathrm{mix}} = \int_\Omega \rho\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u} \, \mathrm{d}V ]

势能 [ \mathcal{G} ] 中应变能对 [ \boldsymbol{u} ] 变分，利用应力定义 [ \boldsymbol{\sigma} = \frac{\partial\psi}{\partial\boldsymbol{\varepsilon}} = \mathbb{D}:\boldsymbol{\varepsilon} ]：

[ \delta_{\boldsymbol{u}}\mathcal{G} = \int_\Omega \boldsymbol{\sigma}:\boldsymbol{\varepsilon}(\delta\boldsymbol{u}) \, \mathrm{d}V - \int_\Omega \bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u} \, \mathrm{d}V - \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u} \, \mathrm{d}\Gamma ]

合并得公式 (13) 的第二式：

[ \delta_{\boldsymbol{u}}H = \int_I\int_\Omega \rho\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u} \, \mathrm{d}V \, \mathrm{d}t - \int_I\left(\int_\Omega\boldsymbol{\sigma}:\boldsymbol{\varepsilon}(\delta\boldsymbol{u}) \, \mathrm{d}V - \int_\Omega\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u} \, \mathrm{d}V - \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u} \, \mathrm{d}\Gamma\right)\mathrm{d}t = 0 \tag{13b} ]

**用到的数学事实**：
- **方向导数（Gâteaux 导数）**：[ \delta_{\boldsymbol{u}}H = \lim_{\varepsilon\to 0}\frac{H[\boldsymbol{u}+\varepsilon\delta\boldsymbol{u}]-H[\boldsymbol{u}]}{\varepsilon} ]
- 应力公式：[ \boldsymbol{\sigma}:\boldsymbol{\varepsilon}(\delta\boldsymbol{u}) = \mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u}):\boldsymbol{\varepsilon}(\delta\boldsymbol{u}) = \delta_{\boldsymbol{u}}\psi ]（链式法则 + 弹性双线性对称性）

---

### 2.3 时间分部积分（公式 (14)）

对 (13b) 中的 [ \int_I\int_\Omega\rho\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u} \, \mathrm{d}V\,\mathrm{d}t ] 进行时间方向**分部积分**：

[ \int_I \rho\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u} \, \mathrm{d}t = \left[\rho\boldsymbol{v}\cdot\delta\boldsymbol{u}\right]_0^T - \int_I \rho\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u} \, \mathrm{d}t ]

利用**端点条件**：[ \delta\boldsymbol{u}\big|_0 = \delta\boldsymbol{u}\big|_T \equiv \mathbf{0} ]（Hamilton 原理要求位移在初末时刻的变分为零），边界项消失。同样对 (13a) 中 [ \rho\nabla_t\boldsymbol{u}\cdot\delta\boldsymbol{v} ] 做分部积分（类似处理）后，得到最终弱形式：

[ \begin{cases} \displaystyle\int_I\int_\Omega -\rho\boldsymbol{v}\cdot\delta\boldsymbol{v} \, \mathrm{d}V\,\mathrm{d}t + \int_I\int_\Omega \rho\nabla_t\boldsymbol{u}\cdot\delta\boldsymbol{v} \, \mathrm{d}V\,\mathrm{d}t = 0 & \forall\delta\boldsymbol{v} \\[8pt] \displaystyle\int_I\int_\Omega \rho\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u} \, \mathrm{d}V\,\mathrm{d}t + \int_I\int_\Omega \boldsymbol{\sigma}:\delta\nabla^{\mathrm{sym}}\boldsymbol{u} \, \mathrm{d}V\,\mathrm{d}t = \int_I\!\!\left(\int_\Omega\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u} \, \mathrm{d}V + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u} \, \mathrm{d}\Gamma\right)\!\mathrm{d}t & \forall\delta\boldsymbol{u} \end{cases} \tag{14} ]

**用到的数学事实**：
- **时间分部积分公式**：[ \int_a^b f'(t)g(t)\,\mathrm{d}t = [f(t)g(t)]_a^b - \int_a^b f(t)g'(t)\,\mathrm{d}t ]
- **Hamilton 变分端点条件**：[ \delta\boldsymbol{u}|_{t=0} = \delta\boldsymbol{u}|_{t=T} = 0 ]，这是 Hamilton 原理的本质约束，使得边界项消失

---

## 三、空时双线性形式与迎风稳定化（§2.2）

### 3.1 双线性形式（公式 (15)–(17)）

将 (14) 整理为**双线性形式**。定义：

[ A(\boldsymbol{v},\boldsymbol{u};\delta\boldsymbol{v},\delta\boldsymbol{u}) = \begin{pmatrix} A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v}) + A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u}) \\ A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v}) + A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u}) \end{pmatrix} = \begin{pmatrix} \mathbf{0} \\ l(\delta\boldsymbol{u}) \end{pmatrix} \tag{15} ]

其中各项分别为（对应 (14) 中四个积分项）：

[ \begin{aligned} A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v}) &= \int_0^T\int_\Omega -\rho\,\delta\boldsymbol{v}\cdot\boldsymbol{v} \, \mathrm{d}\Omega\,\mathrm{d}t \\[4pt] A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u}) &= \int_0^T\int_\Omega \rho\,\delta\boldsymbol{v}\cdot\nabla_t\boldsymbol{u} \, \mathrm{d}\Omega\,\mathrm{d}t \\[4pt] A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v}) &= \int_0^T\int_\Omega \rho\,\delta\boldsymbol{u}\cdot\nabla_t\boldsymbol{v} \, \mathrm{d}\Omega\,\mathrm{d}t \\[4pt] A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_0^T\int_\Omega \boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u}) \, \mathrm{d}\Omega\,\mathrm{d}t \end{aligned} \tag{16} ]

右端**线性泛函**：

[ l(\delta\boldsymbol{u}) = \int_0^T\left(\int_\Omega \bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u} \, \mathrm{d}\Omega + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot\delta\boldsymbol{u} \, \mathrm{d}\Gamma\right)\mathrm{d}t \tag{17} ]

**注意**：第一行方程（对 [ \delta\boldsymbol{v} ] 的方程）右端为零向量，这反映了速度作为独立变量的约束方程：[ \boldsymbol{v} = \nabla_t\boldsymbol{u} ]（弱意义下）。第二行是弹性动力学的动量方程弱形式。

---

### 3.2 时间迎风稳定化（公式 (18)–(19)）

直接使用 (15) 在某些参数下数值不稳定，因此用**时间迎风方法**对测试函数加扰动：

[ \delta\boldsymbol{u} \;\longrightarrow\; \delta\boldsymbol{u} + \theta h \nabla_t(\delta\boldsymbol{u}), \qquad \delta\boldsymbol{v} \;\longrightarrow\; \delta\boldsymbol{v} + \theta h \nabla_t(\delta\boldsymbol{v}) ]

其中 [ \theta > 0 ] 为稳定参数，[ h ] 为时间方向单元尺度。代入 (16) 后得到加稳定项的双线性形式：

[ \begin{aligned} A_{\boldsymbol{vv}}(\delta\boldsymbol{v},\boldsymbol{v}) &= \int_0^T\int_\Omega -\rho(\delta\boldsymbol{v}+\theta h\nabla_t\delta\boldsymbol{v})\cdot\boldsymbol{v} \, \mathrm{d}\Omega\,\mathrm{d}t \\[4pt] A_{\boldsymbol{vu}}(\delta\boldsymbol{v},\boldsymbol{u}) &= \int_0^T\int_\Omega \rho(\delta\boldsymbol{v}+\theta h\nabla_t\delta\boldsymbol{v})\cdot\nabla_t\boldsymbol{u} \, \mathrm{d}\Omega\,\mathrm{d}t \\[4pt] A_{\boldsymbol{uv}}(\delta\boldsymbol{u},\boldsymbol{v}) &= \int_0^T\int_\Omega \rho(\delta\boldsymbol{u}+\theta h\nabla_t\delta\boldsymbol{u})\cdot\nabla_t\boldsymbol{v} \, \mathrm{d}\Omega\,\mathrm{d}t \\[4pt] A_{\boldsymbol{uu}}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_0^T\int_\Omega \boldsymbol{\varepsilon}(\delta\boldsymbol{u}+\theta h\nabla_t\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u}) \, \mathrm{d}\Omega\,\mathrm{d}t \end{aligned} \tag{18} ]

含稳定项的右端：

[ l(\delta\boldsymbol{u}) = \int_0^T\!\!\left(\int_\Omega\bar{\boldsymbol{f}}\cdot(\delta\boldsymbol{u}+\theta h\nabla_t\delta\boldsymbol{u})\,\mathrm{d}\Omega + \int_{\Gamma_\sigma}\bar{\boldsymbol{t}}\cdot(\delta\boldsymbol{u}+\theta h\nabla_t\delta\boldsymbol{u})\,\mathrm{d}\Gamma\right)\!\mathrm{d}t \tag{19} ]

**数学事实（迎风稳定的数学机制）**：

迎风方法源自双曲守恒律的数值分析。对波传播问题，将测试函数改为 [ \delta\boldsymbol{u} + \theta h\nabla_t\delta\boldsymbol{u} ] 等价于在弱形式中引入附加扩散项

[ \theta h \int\rho\nabla_t\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t ]

（即在 [ A_{\boldsymbol{uv}} ] 中展开）。这一项起到人工粘性的作用，提供了正定的稳定贡献，从而压制数值振荡。参数 [ \theta ] 越大稳定性越强，但同时会引入额外阻尼，使能量略有损耗。

---

## 四、VEM 空间离散——投影算子（§3.2）

### 4.1 [ H^1 ] Ritz 投影算子（公式 (20)–(24)）

设多边形单元 [ E ]，[ k ] 阶 VEM 的 **[ H^1 ] Ritz 投影** [ \Pi_\nabla^{k,E}: H^1(E) \to P_k(E) ] 由以下问题唯一确定：

[ a_E\!\left(w - \Pi_\nabla^{k,E}w,\; q_k\right) = 0 \quad \forall q_k \in P_k(E) \tag{20} ]

其中 [ a_E(p,q) = \int_E \nabla p \cdot \nabla q \, \mathrm{d}\Omega ]。

**注意**：(20) 本身只能确定投影在梯度意义下与多项式"最佳逼近"，但有一个常数的不确定性（[ P_k ] 的零空间包含常数函数）。为固定常数，需追加**约束条件**：

[ \begin{cases} \dfrac{1}{N_V^E}\displaystyle\sum_{i=1}^{N_V^E}\left(\Pi_\nabla^{k,E}w - w\right)(x_i) = 0, & k=1 \\[8pt] \dfrac{1}{|E|}\displaystyle\int_E\left(\Pi_\nabla^{k,E}w - w\right)\mathrm{d}\Omega = 0, & k>1 \end{cases} \tag{21} ]

[ k=1 ] 时用**顶点均值**约束；[ k>1 ] 时用**单元均值**约束。

**数学事实**：
- [ a_E(\cdot,\cdot) ] 在商空间 [ H^1/\mathbb{R} ] 上是**强制**（coercive）的（Poincaré 不等式），因此 Ritz 投影在去掉常数的不确定性后唯一存在。
- **Poincaré–Wirtinger 不等式**：对 [ u\in H^1(E) ] 满足 [ \int_E u\,\mathrm{d}\Omega = 0 ]，有 [ \|u\|_{L^2} \leq C_E\|\nabla u\|_{L^2} ]

---

**虚元空间的定义**（公式 (22)–(23)）：

[ \mathcal{V}_h^E := \{ v \in H^1(E) \;:\; v|_{\partial E} \in \mathcal{B}(\partial E),\;\Delta v \in P_{k-2}(E) \} \tag{22} ]

其中边界元空间为：

[ \mathcal{B}(\partial E) := \{ v \in C^0(\partial E) \;:\; v|_F \in P_k(F),\;\forall F \in \partial E \} \tag{23} ]

**VEM 的核心思想**：函数 [ v\in\mathcal{V}_h^E ] 在内部满足调和（或近似调和）方程，其内部值**无需显式计算**——所有计算通过边界信息和投影完成。这是 VEM 与 FEM 最本质的区别。

---

**展开表示（公式 (24)）**：

在矩阵形式下，令 [ \boldsymbol{\phi} ] 为 VEM 空间 [ \mathcal{V}_h^E ] 的形函数向量，[ \boldsymbol{m}_k ] 为 [ P_k(E) ] 的单项式基函数向量（缩放单项式），则：

[ v_h = \boldsymbol{\phi}^T \hat{v}_h, \qquad \Pi_\nabla^{k,E}v_h = \boldsymbol{m}_k^T\,\boldsymbol{\Pi}_\nabla^{k,E*}\hat{v}_h = \boldsymbol{\phi}^T\boldsymbol{\Pi}_\nabla^{k,E}\hat{v}_h \tag{24} ]

其中 [ \hat{v}_h ] 为自由度（顶点值等），[ \boldsymbol{\Pi}_\nabla^{k,E} ] 是投影算子的矩阵表示，[ \boldsymbol{\Pi}_\nabla^{k,E*} ] 是从 DOF 到多项式系数的映射矩阵（满足 [ \boldsymbol{\Pi}_\nabla^{k,E} = D\boldsymbol{\Pi}_\nabla^{k,E*} ]，其中 [ D ] 为 [ \boldsymbol{m}_k ] 在 DOF 处的取值矩阵）。

---

### 4.2 [ L^2 ] 正交投影算子（公式 (25)）

为计算质量矩阵（与速度的 [ L^2 ] 内积），还需要 **[ L^2 ] 正交投影** [ \Pi_{k,E}^0: L^2(E)\to P_k(E) ]：

[ \left(v - \Pi_{k,E}^0 v,\; q_k\right)_E = 0 \quad \forall q_k \in P_k(E) \tag{25} ]

其中 [ (f,g)_E = \int_E fg\,\mathrm{d}\Omega ]。

**重要结论**：当 [ k \leq 2 ] 时，[ \Pi_\nabla^{k,E} = \Pi_{k,E}^0 ]（可通过构造证明）。因此本文 [ k=1 ] 情况下，两种投影相同，无需额外计算 [ L^2 ] 投影。

**数学事实**：
- [ L^2 ] 正交投影是 Hilbert 空间 [ L^2(E) ] 到闭子空间 [ P_k(E) ] 上的**正交投影**，由 Hilbert 空间基本定理保证存在唯一性
- **Galerkin 正交性**：(25) 等价于 [ \|v-\Pi_{k,E}^0 v\|_{L^2} = \min_{p_k\in P_k}\|v-p_k\|_{L^2} ]

---

## 五、离散双线性形式（§3.3）

### 5.1 可计算近似 [ a_h^E ] 和 [ m_h^E ]（公式 (27)–(31)）

VEM 的核心困难：函数 [ v\in\mathcal{V}_h^E ] 的内部值不可显式求得，因此双线性形式

[ a^E(p,q) = \int_E\nabla p\cdot\nabla q\,\mathrm{d}\Omega, \quad m^E(p,q) = \int_E pq\,\mathrm{d}\Omega ]

不能直接计算。解决方法：**用投影替换被积函数**，并加入稳定项弥补非多项式部分：

[ \begin{aligned} a_h^E(p_h,q_h) &:= a^E(\Pi_\nabla^{k,E}p_h,\Pi_\nabla^{k,E}q_h) + S^E\!\!\left((\mathbf{I}-\Pi_\nabla^{k,E})\hat{p}_h,\,(\mathbf{I}-\Pi_\nabla^{k,E})\hat{q}_h\right) \\[4pt] m_h^E(p_h,q_h) &:= m^E(\Pi_{k,E}^0p_h,\Pi_{k,E}^0q_h) + R^E\!\!\left((\mathbf{I}-\Pi_{k,E}^0)\hat{p}_h,\,(\mathbf{I}-\Pi_{k,E}^0)\hat{q}_h\right) \end{aligned} \tag{27} ]

**设计原则**（两条基本要求）：
1. **多项式一致性**：若 [ p_h = q_k\in P_k ]，则 [ a_h^E(q_k,\cdot) = a^E(q_k,\cdot) ]（精确复现多项式的计算）
2. **稳定性**：[ a_h^E(v_h,v_h) \sim a^E(v_h,v_h) ]（等效于真实双线性形式，保证刚度矩阵正定）

---

**[ a_h^E ] 的矩阵表达（公式 (28) 推导）**

**第一项（一致性项）**：因为 [ \Pi_\nabla^{k,E}p_h ] 是多项式，其梯度可显式计算：

[ \nabla(\Pi_\nabla^{k,E}p_h) = \nabla(\boldsymbol{m}_k^T\boldsymbol{\Pi}_\nabla^{k,E*}\hat{p}_h) = (\nabla\boldsymbol{m}_k)^T\boldsymbol{\Pi}_\nabla^{k,E*}\hat{p}_h ]

代入积分：

[ a^E(\Pi_\nabla^{k,E}p_h, \Pi_\nabla^{k,E}q_h) = \hat{p}_h^T(\boldsymbol{\Pi}_\nabla^{k,E*})^T\underbrace{\int_E\nabla\boldsymbol{m}_k(\nabla\boldsymbol{m}_k)^T\,\mathrm{d}\Omega}_{=:\mathbf{G}_\nabla}\boldsymbol{\Pi}_\nabla^{k,E*}\hat{q}_h =: \hat{p}_h^T\mathbf{K}_c^\nabla\hat{q}_h ]

**第二项（稳定项）**：取 [ S^E(\hat{p},\hat{q}) = \hat{p}^T(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})\hat{q} ]，则

[ S^E\!\left((\mathbf{I}-\Pi_\nabla^{k,E})\hat{p}_h,\,(\mathbf{I}-\Pi_\nabla^{k,E})\hat{q}_h\right) = \hat{p}_h^T(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})\hat{q}_h =: \hat{p}_h^T\mathbf{K}_s^\nabla\hat{q}_h ]

合并得（公式 (28)）：

[ a_h^E(p_h,q_h) = \hat{p}_h^T\left[\underbrace{(\boldsymbol{\Pi}_\nabla^{k,E*})^T\int_E\nabla\boldsymbol{m}_k(\nabla\boldsymbol{m}_k)^T\mathrm{d}\Omega\,\boldsymbol{\Pi}_\nabla^{k,E*}}_{\mathbf{K}_c^\nabla} + \underbrace{(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})}_{\mathbf{K}_s^\nabla}\right]\hat{q}_h = \hat{p}_h^T\mathbf{K}_\nabla\hat{q}_h \tag{28} ]

类似地（公式 (29)）：

[ m_h^E(p_h,q_h) = \hat{p}_h^T\left[(\boldsymbol{\Pi}_{k,E}^{0*})^T\int_E\boldsymbol{m}_k\boldsymbol{m}_k^T\mathrm{d}\Omega\,\boldsymbol{\Pi}_{k,E}^{0*} + |E|(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^0)^T(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^0)\right]\hat{q}_h = \hat{p}_h^T\mathbf{K}_0\hat{q}_h \tag{29} ]

其中各矩阵定义（公式 (30)–(31)）：

[ \begin{aligned} \mathbf{K}_c^0 &:= (\boldsymbol{\Pi}_{k,E}^{0*})^T\int_E\boldsymbol{m}_k\boldsymbol{m}_k^T\mathrm{d}\Omega\,\boldsymbol{\Pi}_{k,E}^{0*}, \quad \mathbf{K}_s^0 := |E|(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^0)^T(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^0), \quad \mathbf{K}_0 := \mathbf{K}_c^0+\mathbf{K}_s^0 \end{aligned} \tag{30} ]

[ \begin{aligned} \mathbf{K}_c^\nabla &:= (\boldsymbol{\Pi}_\nabla^{k,E*})^T\int_E\nabla\boldsymbol{m}(\nabla\boldsymbol{m})^T\mathrm{d}\Omega\,\boldsymbol{\Pi}_\nabla^{k,E*}, \quad \mathbf{K}_s^\nabla := (\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E})^T(\mathbf{I}-\boldsymbol{\Pi}_\nabla^{k,E}), \quad \mathbf{K}_\nabla := \mathbf{K}_c^\nabla+\mathbf{K}_s^\nabla \end{aligned} \tag{31} ]

**用到的数学事实**：
- **最优性**：稳定项 [ \hat{p}^T\mathbf{K}_s\hat{q} = \hat{p}^T(\mathbf{I}-\Pi)^T(\mathbf{I}-\Pi)\hat{q} = \langle(\mathbf{I}-\Pi)\hat{p},(\mathbf{I}-\Pi)\hat{q}\rangle ] 是半正定的，仅对非多项式部分有贡献（因为对 [ \hat{p}\in P_k ] 有 [ (\mathbf{I}-\Pi)\hat{p}=0 ]）
- **等价范数**：在适当选取稳定双线性形式 [ S^E ] 时，[ a_h^E \sim a^E ]（谱等价），保证 VEM 的最优收敛率

---

### 5.2 向量场位移的双线性形式（公式 (32)–(37)）

**位移分量拆分（公式 (32)）**：

[ \boldsymbol{u} = [u_x,\, u_y]^T, \quad \delta\boldsymbol{u} = [\delta u_x,\, \delta u_y]^T \tag{32} ]

对每个分量分别应用标量 [ m_h^E ]，得到速度场的质量双线性形式（公式 (33)）：

[ m_h^E(\delta\boldsymbol{v},\boldsymbol{v}) = \begin{bmatrix}\delta\hat{v}_x^T & \delta\hat{v}_y^T\end{bmatrix}\begin{bmatrix}\mathbf{K}_0 & \\ & \mathbf{K}_0\end{bmatrix}\begin{bmatrix}\hat{v}_x\\\hat{v}_y\end{bmatrix} = \delta\hat{\boldsymbol{v}}^T\mathbb{K}_0\hat{\boldsymbol{v}}, \quad \mathbb{K}_0 := \mathrm{diag}(\mathbf{K}_0,\mathbf{K}_0) \tag{33} ]

---

**Voigt 应变表示（公式 (34)–(35)）**：

将应变张量 [ \boldsymbol{\varepsilon} ] 用 Voigt 列向量 [ \tilde{\boldsymbol{\varepsilon}} = [\varepsilon_{xx},\varepsilon_{yy},2\varepsilon_{xy}]^T ] 表示：

[ \tilde{\boldsymbol{\varepsilon}}(\boldsymbol{u}) = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u}) \tag{34} ]

其中提取矩阵 [ \mathbf{A} ] 和位移梯度向量 [ \boldsymbol{d}(\boldsymbol{u}) ] 为：

[ \mathbf{A} = \begin{bmatrix}1&0&0&0\\0&0&0&1\\0&1&1&0\end{bmatrix}, \quad \boldsymbol{d}(\boldsymbol{u}) = \begin{bmatrix}\partial u_x/\partial x \\ \partial u_x/\partial y \\ \partial u_y/\partial x \\ \partial u_y/\partial y\end{bmatrix} \tag{35} ]

**推导验证**：

[ \mathbf{A}\boldsymbol{d}(\boldsymbol{u}) = \begin{bmatrix}1\cdot\frac{\partial u_x}{\partial x}\\1\cdot\frac{\partial u_y}{\partial y}\\\frac{\partial u_x}{\partial y}+\frac{\partial u_y}{\partial x}\end{bmatrix} = \begin{bmatrix}\varepsilon_{xx}\\\varepsilon_{yy}\\2\varepsilon_{xy}\end{bmatrix} = \tilde{\boldsymbol{\varepsilon}} ]

与应变定义 (8) 完全一致。

---

**向量场投影（公式 (36)）**：

对位移向量 [ \boldsymbol{u}_h = [u_h^x, u_h^y]^T ]，将投影分量对角化：

[ \Pi_\nabla^{k,E}\boldsymbol{u}_h = \begin{bmatrix}\boldsymbol{m}^T & \\ & \boldsymbol{m}^T\end{bmatrix}\begin{bmatrix}\boldsymbol{\Pi}_\nabla^{k,E*} & \\ & \boldsymbol{\Pi}_\nabla^{k,E*}\end{bmatrix}\begin{bmatrix}\hat{u}_h^x\\\hat{u}_h^y\end{bmatrix} = \mathbf{M}^T\boldsymbol{\Pi}_\nabla^*\hat{\boldsymbol{u}}_h \tag{36} ]

其中 [ \mathbf{M}^T = \mathrm{diag}(\boldsymbol{m}^T,\boldsymbol{m}^T) ]，[ \boldsymbol{\Pi}_\nabla^* = \mathrm{diag}(\boldsymbol{\Pi}_\nabla^{k,E*},\boldsymbol{\Pi}_\nabla^{k,E*}) ]。

---

**应变能双线性形式（公式 (37) 严格推导）**

目标：推导 [ a_h^E(\delta\boldsymbol{u},\boldsymbol{u}) = \int_E\boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega ] 的矩阵形式。

**步骤 1**：用 Voigt 记号将双缩并写为矩阵乘法：

[ \boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u}) = \tilde{\boldsymbol{\varepsilon}}(\delta\boldsymbol{u})^T\tilde{\mathbb{D}}\,\tilde{\boldsymbol{\varepsilon}}(\boldsymbol{u}) = \boldsymbol{d}(\delta\boldsymbol{u})^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}\,\boldsymbol{d}(\boldsymbol{u}) ]

其中 [ \tilde{\mathbb{D}} ] 是弹性张量 [ \mathbb{D} ] 的 Voigt 矩阵（[ 3\times 3 ] 或 [ 6\times 6 ]）。

**步骤 2**：利用 [ \Pi_\nabla ] 替换 [ \boldsymbol{u}_h ]（VEM 可计算性的关键步骤）：

[ \boldsymbol{d}(\Pi_\nabla^{k,E}\boldsymbol{u}_h) = (\nabla\mathbf{M})\boldsymbol{\Pi}_\nabla^*\hat{\boldsymbol{u}}_h ]

其中 [ \nabla\mathbf{M} ] 是 [ \mathbf{M}^T ] 的梯度矩阵（[ 4\times 2N_{DOF} ] 形式）。

**步骤 3**：代入积分（一致性项）：

[ a^E(\Pi_\nabla^*\boldsymbol{u}_h,\Pi_\nabla^*\delta\boldsymbol{u}_h) = \delta\hat{\boldsymbol{u}}^T(\boldsymbol{\Pi}_\nabla^*)^T\underbrace{\int_E(\nabla\mathbf{M})^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}(\nabla\mathbf{M})\,\mathrm{d}\Omega}_{=:\mathbf{G}_\varepsilon}\boldsymbol{\Pi}_\nabla^*\hat{\boldsymbol{u}} = \delta\hat{\boldsymbol{u}}^T\mathbf{K}_c^\varepsilon\hat{\boldsymbol{u}} ]

**步骤 4**：加稳定项，定义稳定参数 [ \gamma = \frac{1}{4N_K}\mathrm{tr}(\mathbf{K}_c^\varepsilon) ]：

[ a_h^E(\delta\boldsymbol{u},\boldsymbol{u}) = \delta\hat{\boldsymbol{u}}^T\!\left(\mathbf{K}_c^\varepsilon + \gamma\begin{bmatrix}\mathbf{K}_s^\nabla & \\ & \mathbf{K}_s^\nabla\end{bmatrix}\right)\!\hat{\boldsymbol{u}} = \delta\hat{\boldsymbol{u}}^T\!\left(\mathbf{K}_c^\varepsilon + \gamma\mathbf{K}_s^\varepsilon\right)\!\hat{\boldsymbol{u}} = \delta\hat{\boldsymbol{u}}^T\mathbf{K}_\varepsilon\hat{\boldsymbol{u}} \tag{37} ]

**用到的数学事实**：
- **Voigt 记号**：将对称张量缩并化为矩阵向量乘法，利用 [ \varepsilon_{ij} = \varepsilon_{ji} ] 减少存储量：[ \boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} = \tilde{\boldsymbol{\varepsilon}}^T\tilde{\mathbb{D}}\tilde{\boldsymbol{\varepsilon}} ]
- [ \tilde{\mathbb{D}} ] 的正定性（线弹性材料假设）：保证刚度矩阵 [ \mathbf{K}_\varepsilon ] 正定
- 稳定参数的选取：[ \gamma\sim \frac{1}{N_K}\mathrm{tr}(\mathbf{K}_c^\varepsilon) ] 保证稳定项与一致性项等阶，从而不破坏整体精度

---

## 六、时空矩阵的张量积结构（§3.4）

### 6.1 时空单元形函数（公式 (38)–(40)）

时间区间 [ [t_n, t_{n+1}] ] 上采用**线性 Lagrange 插值**，参考坐标 [ \xi\in[-1,1] ]：

[ \Phi_{\kappa_t}(1) = \frac{1-\xi}{2}, \quad \Phi_{\kappa_t}(2) = \frac{1+\xi}{2} \tag{39} ]

**验证**：[ \Phi_{\kappa_t}(1)|_{\xi=-1} = 1 ]，[ \Phi_{\kappa_t}(1)|_{\xi=1} = 0 ]（符合线性有限元的形函数性质）。时间导数：

[ \nabla_t\Phi_{\kappa_t}(1) = -\frac{1}{h}, \quad \nabla_t\Phi_{\kappa_t}(2) = \frac{1}{h} ]

（其中 [ h = t_{n+1}-t_n ] 为时间步长；利用 [ \xi = \frac{2(t-t_n)}{h}-1 ]，则 [ \frac{\mathrm{d}}{\mathrm{d}t} = \frac{2}{h}\frac{\mathrm{d}}{\mathrm{d}\xi} ]）

**时空形函数**（空间 VEM 形函数 × 时间 FEM 形函数，即张量积）：

[ \Phi_\kappa = \begin{bmatrix}\Phi_{\kappa_x}\Phi_{\kappa_t}(1)\\\Phi_{\kappa_x}\Phi_{\kappa_t}(2)\end{bmatrix} \tag{38} ]

其梯度（链式法则）：

[ \nabla_x\Phi_\kappa = \begin{bmatrix}\nabla_x\Phi_{\kappa_x}\Phi_{\kappa_t}(1)\\\nabla_x\Phi_{\kappa_x}\Phi_{\kappa_t}(2)\end{bmatrix}, \quad \nabla_t\Phi_\kappa = \begin{bmatrix}\Phi_{\kappa_x}\nabla_t\Phi_{\kappa_t}(1)\\\Phi_{\kappa_x}\nabla_t\Phi_{\kappa_t}(2)\end{bmatrix} \tag{40} ]

**数学事实**：张量积形函数的梯度满足乘积法则 [ \nabla(fg) = (\nabla f)g + f(\nabla g) ]；由于空间形函数不依赖 [ t ]，时间导数仅作用于时间形函数部分。

---

**时空位移插值（公式 (41)–(42)）**：

[ U_h = \begin{bmatrix}\Phi_K^T & \\ & \Phi_K^T\end{bmatrix}\hat{U}_h =: \boldsymbol{\Phi}_K^T\hat{U}_h \tag{41} ]

自由度向量（每个时间层的 x/y 分量）：

[ \hat{U}_h = \begin{bmatrix}\hat{u}_x^b & \hat{u}_x^t & \hat{u}_y^b & \hat{u}_y^t\end{bmatrix}^T \tag{42} ]

其中上标 [ b/t ] 分别表示时间单元的底面（[ t=t_n ]）和顶面（[ t=t_{n+1} ]）。

---

### 6.2 单元刚度矩阵的 Kronecker 积结构（公式 (43)–(54)）

**分块结构（公式 (43)）**：

[ \mathbf{K} = \begin{bmatrix}\mathbf{K}_{vv} & \mathbf{K}_{vu}\\\mathbf{K}_{uv} & \mathbf{K}_{uu}\end{bmatrix} \tag{43} ]

**严格推导 [ \mathbf{K}_{vv} ] 的 Kronecker 积结构（公式 (44)–(51)）**

[ \mathbf{K}_{vv} ] 来自双线性形式 [ A_{\boldsymbol{vv}}(\delta\boldsymbol{v},\boldsymbol{v}) = \int\int -\rho(\delta\boldsymbol{v}+\theta h\nabla_t\delta\boldsymbol{v})\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t ]。

将速度和测试函数用张量积形函数展开：[ \boldsymbol{v}_h = \Phi_\kappa^T\hat{v} ]，则

[ \int_{t_n}^{t_{n+1}}\int_{\mathcal{K}_x}\Phi_\kappa(\cdot)\Phi_\kappa^T\,\mathrm{d}\Omega\,\mathrm{d}t ]

利用 [ \Phi_\kappa = \Phi_{\kappa_x}\otimes\Phi_{\kappa_t} ]（张量积结构），可以分离空间积分和时间积分：

[ \int_{t_n}^{t_{n+1}}\int_{\mathcal{K}_x}\Phi_\kappa\Phi_\kappa^T\,\mathrm{d}\Omega\,\mathrm{d}t = \left(\int_{t_n}^{t_{n+1}}\Phi_{\kappa_t}\Phi_{\kappa_t}^T\,\mathrm{d}t\right)\otimes\left(\int_{\mathcal{K}_x}\Phi_{\kappa_x}\Phi_{\kappa_x}^T\,\mathrm{d}\Omega\right) ]

**关键数学事实**：设 [ A = (a_{ij}) ]，[ B = (b_{kl}) ]，Kronecker 积 [ A\otimes B = (a_{ij}B) ]。对于张量积形函数，空间—时间可分离性导致：

[ \int_{\Omega\times I}(f(x)\cdot g(t))(f(x)\cdot g(t))^T\,\mathrm{d}x\,\mathrm{d}t = \left(\int_I g(t)g(t)^T\mathrm{d}t\right)\otimes\left(\int_\Omega f(x)f(x)^T\mathrm{d}x\right) ]

这正是 Fubini 定理 + Kronecker 积乘法规则的体现。

空间积分部分即为 [ \mathbf{K}_0 ]（公式 (45)）：

[ \int_{\mathcal{K}_x}\boldsymbol{\phi}_{\kappa_x}\boldsymbol{\phi}_{\kappa_x}^T\,\mathrm{d}\Omega = \mathbf{K}_c^0 + \mathbf{K}_s^0 =: \mathbf{K}_0 \tag{45} ]

时间方向积分用 2 点 Gauss 积分（[ \xi\in[-1,1] ]，换元 [ t = t_n + \frac{h}{2}(\xi+1) ]，Jacobian [ \frac{h}{2} ]）：

[ \int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\kappa_t}\boldsymbol{\phi}_{\kappa_t}^T\,\mathrm{d}t = \frac{h}{2}\int_{-1}^{1}\boldsymbol{\phi}_{\kappa_t}\boldsymbol{\phi}_{\kappa_t}^T\,\mathrm{d}\xi \approx \frac{h}{2}\sum_{i=1}^{2}w_i\boldsymbol{\phi}_{\kappa_t}(\xi_i)\boldsymbol{\phi}_{\kappa_t}^T(\xi_i) =: \mathbf{L} \tag{46} ]

[ \int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\kappa_t}\boldsymbol{\phi}_{\kappa_t}^T\,\mathrm{d}t = \frac{h}{2}\sum_{i=1}^{2}w_i\,\nabla_t\boldsymbol{\phi}_{\kappa_t}(\xi_i)\boldsymbol{\phi}_{\kappa_t}^T(\xi_i) =: \mathbf{Q} \tag{47} ]

[ \int_{t_n}^{t_{n+1}}\boldsymbol{\phi}_{\kappa_t}\nabla_t\boldsymbol{\phi}_{\kappa_t}^T\,\mathrm{d}t = \frac{h}{2}\sum_{i=1}^{2}w_i\,\boldsymbol{\phi}_{\kappa_t}(\xi_i)\nabla_t\boldsymbol{\phi}_{\kappa_t}^T(\xi_i) =: \mathbf{R} \tag{48} ]

[ \int_{t_n}^{t_{n+1}}\nabla_t\boldsymbol{\phi}_{\kappa_t}\nabla_t\boldsymbol{\phi}_{\kappa_t}^T\,\mathrm{d}t = \frac{h}{2}\sum_{i=1}^{2}w_i\,\nabla_t\boldsymbol{\phi}_{\kappa_t}(\xi_i)\nabla_t\boldsymbol{\phi}_{\kappa_t}^T(\xi_i) =: \mathbf{W} \tag{49} ]

**[ \mathbf{L},\mathbf{Q},\mathbf{R},\mathbf{W} ] 的精确值（[ 2\times 2 ] 矩阵）**：

对线性形函数 [ \phi_1 = (1-\xi)/2 ]，[ \phi_2 = (1+\xi)/2 ]，[ \nabla_t\phi_1 = -1/h ]，[ \nabla_t\phi_2 = 1/h ]，2 点 Gauss 积分（[ \xi_1 = -1/\sqrt{3} ]，[ \xi_2 = 1/\sqrt{3} ]，权 [ w_1=w_2=1 ]）精确计算得：

[ \mathbf{L} = \int_0^h\begin{bmatrix}\phi_1^2 & \phi_1\phi_2\\\phi_2\phi_1 & \phi_2^2\end{bmatrix}\mathrm{d}t = h\begin{bmatrix}1/3 & 1/6\\1/6 & 1/3\end{bmatrix} ]

[ \mathbf{Q} = \int_0^h\begin{bmatrix}\nabla_t\phi_1\cdot\phi_1 & \nabla_t\phi_1\cdot\phi_2\\\nabla_t\phi_2\cdot\phi_1 & \nabla_t\phi_2\cdot\phi_2\end{bmatrix}\mathrm{d}t = \begin{bmatrix}-1/2 & -1/2\\1/2 & 1/2\end{bmatrix} ]

[ \mathbf{R} = \mathbf{Q}^T = \begin{bmatrix}-1/2 & 1/2\\-1/2 & 1/2\end{bmatrix} ]

[ \mathbf{W} = \int_0^h\begin{bmatrix}1/h^2 & -1/h^2\\-1/h^2 & 1/h^2\end{bmatrix}\mathrm{d}t = \frac{1}{h}\begin{bmatrix}1 & -1\\-1 & 1\end{bmatrix} ]

**数学事实**：2 点 Gauss-Legendre 积分对 [ 2k-1 \leq 3 ] 次多项式精确，而上述被积函数最高为 [ \phi_i\phi_j ]（2 次多项式），故用 2 点积分精确。

---

**分块约定（公式 (50)）**

将任意空间矩阵 [ \mathbf{K}_{\mathrm{any}} ] 按行/列（x 分量为 I，y 分量为 II）分成 [ 2\times 2 ] 块：

[ \mathbf{K}_{\mathrm{any}} = \begin{bmatrix}\mathbf{K}_{\mathrm{any}}^{I,I} & \mathbf{K}_{\mathrm{any}}^{I,II}\\\mathbf{K}_{\mathrm{any}}^{II,I} & \mathbf{K}_{\mathrm{any}}^{II,II}\end{bmatrix} \tag{50} ]

---

**完整刚度矩阵块（公式 (51)–(54)）**

将张量积分解代入各双线性形式（含迎风项 [ \theta h ]）：

[ \mathbf{K}_{\boldsymbol{vv}} = \rho^{-1}\begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{L}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \rho^{-1}\theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{Q}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} \tag{51} ]

[ \mathbf{K}_{\boldsymbol{vu}} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{R}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{W}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} \tag{52} ]

[ \mathbf{K}_{\boldsymbol{uv}} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{R}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_0^{I,I} & \\ & \mathbf{W}\otimes\mathbf{K}_0^{II,II}\end{bmatrix} \tag{53} ]

[ \mathbf{K}_{\boldsymbol{uu}} = \begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_\varepsilon^{I,I} & \mathbf{L}\otimes\mathbf{K}_\varepsilon^{I,II}\\\mathbf{L}\otimes\mathbf{K}_\varepsilon^{II,I} & \mathbf{L}\otimes\mathbf{K}_\varepsilon^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_\varepsilon^{I,I} & \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{I,II}\\\mathbf{Q}\otimes\mathbf{K}_\varepsilon^{II,I} & \mathbf{Q}\otimes\mathbf{K}_\varepsilon^{II,II}\end{bmatrix} \tag{54} ]

**关键洞见**：
- 矩阵 [ \mathbf{K}_0 ] 和 [ \mathbf{K}_\varepsilon ] 仅依赖空间域，**只需计算一次**（对每个空间多边形单元）
- 时间积分矩阵 [ \mathbf{L,Q,R,W} ] 对所有单元相同（只依赖时间步长 [ h ]）
- 最终刚度矩阵通过 **Kronecker 积**直接组装，极大简化了编程复杂度

**数学事实（Kronecker 积性质）**：
- [ (A\otimes B)(C\otimes D) = (AC)\otimes(BD) ]（混合乘法）
- [ (A\otimes B)^T = A^T\otimes B^T ]
- 若 [ A ] 为 [ m\times n ] 矩阵，[ B ] 为 [ p\times q ] 矩阵，则 [ A\otimes B ] 为 [ mp\times nq ] 矩阵

---

## 七、有限元法的时空格式（§4）

### 7.1 FEM 质量和刚度矩阵（公式 (55)–(57)）

对有限元法，空间形函数 [ \phi_{\mathrm{FEM}} ] 显式可知，因此质量矩阵和刚度矩阵可直接计算：

**质量矩阵**（对应 [ \mathbf{K}_0 ]）：

[ \mathbf{K}^0 = \int_{\kappa_x}[\phi_{\mathrm{FEM}}][\phi_{\mathrm{FEM}}]^T\,\mathrm{d}\Omega \tag{55} ]

**刚度矩阵**（对应 [ \mathbf{K}_\varepsilon ]）：

[ \mathbf{K}^\varepsilon = \int_{\kappa_x}\mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega \tag{56} ]

其中**应变-位移矩阵** [ \mathbf{B} ] 为：

[ \mathbf{B} := \begin{bmatrix}\dfrac{\partial\phi_{\mathrm{FEM}}^T}{\partial x} & \mathbf{0}^T \\ \mathbf{0}^T & \dfrac{\partial\phi_{\mathrm{FEM}}^T}{\partial y} \\ \dfrac{\partial\phi_{\mathrm{FEM}}^T}{\partial y} & \dfrac{\partial\phi_{\mathrm{FEM}}^T}{\partial x}\end{bmatrix} \tag{57} ]

**说明**：[ \mathbf{B} ] 的列数为 [ 2N_{node} ]（[ N_{node} ] 为单元节点数），[ \mathbf{B}^T\tilde{\mathbb{D}}\mathbf{B} ] 给出应变能的二次型，等价于 VEM 中的 [ (\nabla\mathbf{M})^T\mathbf{A}^T\tilde{\mathbb{D}}\mathbf{A}(\nabla\mathbf{M}) ]。

验证 [ \mathbf{B} ] 的正确性：

[ \mathbf{B}\hat{\boldsymbol{u}} = \begin{bmatrix}\nabla\phi_{\mathrm{FEM}}^T\hat{u}_x \\ \nabla\phi_{\mathrm{FEM}}^T\hat{u}_y \\ \frac{\partial\phi}{\partial y}^T\hat{u}_x + \frac{\partial\phi}{\partial x}^T\hat{u}_y\end{bmatrix} = \begin{bmatrix}\partial_x u_x \\ \partial_y u_y \\ \partial_y u_x + \partial_x u_y\end{bmatrix} = \tilde{\boldsymbol{\varepsilon}} ]

与 Voigt 应变定义完全一致。

将 [ \mathbf{K}^0 ] 和 [ \mathbf{K}^\varepsilon ] 代入公式 (51)–(54) 后，空时刚度矩阵可由完全相同的 Kronecker 积结构组装。

---

## 八、数值算例（§5）

### 8.1 各算例关键参数

| 算例 | 几何 | 材料 | 时间步 [ \Delta t ] | 参数 [ \theta ] |
|:---|:---|:---|:---|:---|
| 悬臂梁 | [ L\times H = 5\times 1 ] | [ E=200000 ]，[ \nu=0.3 ] | [ 0.1\sim 0.2 ] | [ 0.01 ] |
| Cook 膜 | [ L=48,H_1=44,H_2=16 ] | [ E=200000 ]，[ \nu=0.3 ] | [ 0.1\sim 0.2 ] | [ 0.01 ] |
| 含斜裂纹板 | [ 2H\times 2L = 40\times 20 ]，裂纹 [ 2a=5 ] | [ E=200000 ]，[ \nu=0.3 ] | [ 0.02 ] | [ 0.01 ] |
| 任意几何（体力） | 动物形状 | [ E=200000 ]，[ \nu=0.3 ] | [ \Delta t=2 ] | [ 0.01 ] |

### 8.2 稳定参数 [ \theta ] 的物理意义

当 [ \theta = 0 ] 时退化为 Galerkin 方法，可能出现数值振荡；[ \theta > 0 ] 引入等效阻尼，随 [ \theta ] 增大稳定性增强但能量损耗加剧。实验表明 [ \theta = 0.01 ] 在精度与稳定性之间取得最优平衡，对应双曲问题中 Courant 数量级的最优扰动。

---

## 九、方法优势与局限总结

### 优势

| 特性 | 原因 |
|:---|:---|
| 支持任意多边形网格 | VEM 形函数不需要显式计算，仅依赖边界信息和投影 |
| 消除相位误差 | 空时方法将时间视为维度，全局求解无时间步进误差累积 |
| 高效组装 | Kronecker 积结构：空间矩阵 [ \mathbf{K}_0,\mathbf{K}_\varepsilon ] 只需计算一次 |
| 无额外时间方向 DOF | 张量积结构直接复用静力学 VEM 投影算子 |
| 利于并行 | 各时间片可独立处理 |

### 局限

- 全局稀疏线性系统自由度数 [ \propto N_{\mathrm{space}}\times N_T ]，大规模问题内存消耗大
- 仅处理线弹性小应变问题，非线性扩展尚待研究
- 大时间跨度问题需要自适应时间步长（未在本文讨论）

---

## 附录：核心数学工具一览

| 数学工具 | 在论文中的应用 |
|:---|:---|
| **Fubini 定理** | 交换空间与时间积分顺序（公式 (2)，Kronecker 积分离） |
| **Gâteaux 导数（泛函变分）** | 推导 Hamilton 驻值条件（公式 (11)–(13)） |
| **时间分部积分** | 将 [ \int\rho\boldsymbol{v}\cdot\nabla_t\delta\boldsymbol{u} ] 转化为 [ -\int\rho\nabla_t\boldsymbol{v}\cdot\delta\boldsymbol{u} ]（公式 (14)） |
| **Poincaré 不等式** | 保证 [ H^1 ] 半范数是 [ H^1/\mathbb{R} ] 上的范数，从而 [ \Pi_\nabla^{k,E} ] 的适定性 |
| **Galerkin 正交性** | [ L^2 ] 投影的最优逼近性（公式 (25)） |
| **Einstein 求和约定** | 弹性张量缩并（公式 (6)–(7)） |
| **Voigt 记号** | 将 [ 2\times 2 ] 对称张量压缩为 3 维向量，张量积写为矩阵乘法（公式 (34)–(37)） |
| **Kronecker 积分解** | 时空可分变量函数的积分分离（公式 (44)–(54)） |
| **Gauss-Legendre 积分** | 多项式精确积分（公式 (46)–(49)） |
| **Legendre 变换思想** | 引入速度 [ \boldsymbol{v} ] 为独立变量（公式 (10)） |
