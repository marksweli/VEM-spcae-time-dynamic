# 公式代码对应——空时虚元法弹性动力学

> 本文档将论文《Space-time virtual element method for elastodynamics: Theory, applications, and code development》（文献翻译.md）中**所有编号公式**，逐一与仓库中的最底层 MATLAB 计算代码对应，按论文章节分段，以**代码在前、公式在后**的格式进行解读。
>
> 代码仓库：<https://github.com/marksweli/VEM-spcae-time-dynamic>

---

## 2. 变分公式（Variational Formulation）

### 2.1 基于 Hamilton 原理的变分公式

---

#### 公式 (1)：时空柱体 $Q$

**最底层代码** — [`PolyMesh3Simple.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/PolyMesh3Simple.m)（第 23 行）

将空间网格节点沿时间方向 $z$（即 $t$）张量积，构造出时空柱体 $Q = \Omega \times I$ 对应的三维网格：

```matlab
node3 = [repmat(node,J+1,1),  kron(z(:),ones(N,1))];
```

**公式 (1)**

$$[ Q \coloneqq \Omega \times I ]$$

其中 $[ I = (0,T) ]$，时空柱体是空间域 $\Omega$ 与时间区间 $I$ 的笛卡尔积。

---

#### 公式 (2)：Hamilton 作用量泛函 $H$

**最底层代码** — [`main.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/main.m)（第 12、31 行）

整体求解流程体现了对 Hamilton 作用量的离散化：组装全局刚度矩阵后直接求解线性方程组，等价于对 $H$ 求驻值：

```matlab
[GK] = globalK(node,elem,mat,zline,elem3,theta);
% ...
u = GK\f;
```

**公式 (2)**

$$[ H \coloneqq \int_{\Omega}\int_{\boldsymbol{u}_0}^{\boldsymbol{u}_T}\boldsymbol{\rho}\cdot \mathrm{d}\boldsymbol{u}\,\mathrm{d}V = \int_{I}\int_{\Omega}\rho \|\nabla_{t}\boldsymbol{u}\|^{2}\,\mathrm{d}V\,\mathrm{d}t = \int_{I}2\mathcal{K}\,\mathrm{d}t ]$$

其中 $[ \boldsymbol{\rho} = \rho\nabla_t\boldsymbol{u} ]$ 为动量密度向量。

---

#### 公式 (3)：动能 $\mathcal{K}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 52–55 行）

$\mathcal{K}$ 在离散层面对应 $\mathbf{K}_{vv}$ 块。下列代码构造与速度场相关的时间积分质量矩阵 $-\rho^{-1}(\mathbf{L}\otimes\mathbf{K}_0)$（含迎风项），即动能对应的双线性形式：

```matlab
Kpp_c = [Int*NN(1,1), Int*NN(1,2);
         Int*NN(2,1), Int*NN(2,2)] + ...
         theta*h*[Int*dNN(1,1), Int*dNN(1,2);
                  Int*dNN(2,1), Int*dNN(2,2)];
```

**公式 (3)**

$$[ \mathcal{K} = \int_{\Omega}\frac{1}{2}\rho \|\nabla_{t}\boldsymbol{u}\|^{2}\,\mathrm{d}V ]$$

---

#### 公式 (4)：能量守恒方程

**最底层代码** — [`main.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/main.m)（第 31 行）

能量守恒（热力学第一定律）最终体现为全局线性方程 $\mathbf{K}\mathbf{u} = \mathbf{f}$：

```matlab
u = GK\f;
```

**公式 (4)**

$$[ \int_{I}\mathcal{K}\,\mathrm{d}t + \int_{I}\mathcal{G}\,\mathrm{d}t = 0 ]$$

其中 $[ \mathcal{G} ]$ 为总势能。

---

#### 公式 (5)：总势能 $\mathcal{G}$

**最底层代码** — [`getForce.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/getForce.m)（第 61、71 行）

体积力和 Neumann 边界载荷的时间积分对应 $\mathcal{G}$ 中的外力项：

```matlab
P = [p*T(1);p*T(2)] + theta*h*[p*intDf(1);p*intDf(2)];
% ...
ff(faceNodeIDNew+sumNodeNew) = ff(faceNodeIDNew+sumNodeNew) + P;
```

**公式 (5)**

$$[ \mathcal{G} \coloneqq \int_{\Omega}\psi\,\mathrm{d}V - \int_{\Omega}\tilde{\boldsymbol{f}}\cdot\boldsymbol{u}\,\mathrm{d}V - \int_{\Gamma_{\sigma}}\tilde{\boldsymbol{t}}\cdot\boldsymbol{u}\,\mathrm{d}\Gamma ]$$

其中 $[ \tilde{\boldsymbol{f}} ]$ 为体积力，$[ \tilde{\boldsymbol{t}} ]$ 为 Neumann 边界上的外压力。

---

#### 公式 (6)：自由能密度 $\psi$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 14 行）

平面应力假设下弹性张量 $\mathbb{D}$ 的 Voigt 形式 $\tilde{\mathbb{D}}$：

```matlab
D = EX/(1-mu^2)*[1, mu, 0;
                  mu, 1, 0;
                  0,  0, (1-mu)/2];
```

**公式 (6)**

$$[ \psi \coloneqq \frac{\lambda}{2}\left(\mathrm{tr}(\boldsymbol{\varepsilon})\right)^{2} + \mu\,\boldsymbol{\varepsilon}:\boldsymbol{\varepsilon} = \frac{1}{2}\boldsymbol{\varepsilon}:\mathbb{D}:\boldsymbol{\varepsilon} ]$$

$[ \psi ]$ 为线弹性自由能密度，$[ \lambda,\mu ]$ 为 Lamé 常数，$[ \mathbb{D} ]$ 为四阶弹性张量。

---

#### 公式 (7)：弹性张量分量 $\mathbb{D}_{ijkl}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 14 行）

代码矩阵 `D` 正是 $\mathbb{D}_{ijkl}$ 的 Voigt 缩并形式，其中弹性模量 `EX` 和泊松比 `mu` 由输入材料参数提供：

```matlab
EX = mat(1); mu = mat(2); rho = mat(3);
D = EX/(1-mu^2)*[1, mu, 0;
                  mu, 1, 0;
                  0,  0, (1-mu)/2];
```

**公式 (7)**

$$[ \mathbb{D}_{ijkl} = \lambda\delta_{ij}\delta_{kl} + \mu\left(\delta_{ik}\delta_{jl} + \delta_{il}\delta_{jk}\right) ]$$

---

#### 公式 (8)：应变张量 $\boldsymbol{\varepsilon}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 69–73 行）

利用 Voigt 记号，对称梯度算子由矩阵 $\mathbf{A}$ 实现：

```matlab
A1 = [1,0; 0,0; 0,1];
A2 = [0,0; 0,1; 1,0];
A  = [A1, A2];
```

其中 $\mathbf{A}$ 将位移梯度向量 $\boldsymbol{d}(\boldsymbol{u})$ 映射为 Voigt 应变向量 $\tilde{\boldsymbol{\varepsilon}}$，即 $\tilde{\boldsymbol{\varepsilon}} = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u})$（参见公式 (34)-(35)）。

**公式 (8)**

$$[ \boldsymbol{\varepsilon} = \frac{1}{2}\left(\nabla\boldsymbol{u} + \nabla\boldsymbol{u}^{T}\right) =: \nabla^{\mathrm{sym}}\boldsymbol{u} = \boldsymbol{\varepsilon}(\boldsymbol{u}) ]$$

---

#### 公式 (9)：重构的 Hamilton 作用量

**最底层代码** — [`globalK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/globalK.m)（第 36–51 行）

组装全局刚度矩阵的循环体现了将所有单元和时间段的 $(\mathcal{K}-\mathcal{G})$ 贡献叠加的过程：

```matlab
for n = 1:sumElem
    Pi = Pi_all{n}; Pis = Pis_all{n};
    for m = 1:(length(zline)-1)
        AK = elemK(n,node,elem,area(n),centroid(n,:),diameter(n),Pi,Pis,mat,[zline(m),zline(m+1)],theta);
        % 组装到全局稀疏矩阵
        ss(ia+1:ia+Ndof^2) = AB(:);
    end
end
```

**公式 (9)**

$$[ H = \int_{\Omega}\left(\mathcal{K} - \mathcal{G}\right)\mathrm{d}t ]$$

---

#### 公式 (10)：混合运动能（引入速度 $\boldsymbol{v}$ 作为独立变量）

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 52–66 行、第 91 行）

混合公式引入速度 $\boldsymbol{v}$ 为独立变量后，刚度矩阵变为四块结构，下面构造与 $\boldsymbol{v}$ 相关的对角块：

```matlab
Kpp_c = blkdiag(Kpp_c, Kpp_c);  % 对应 x, y 分量
Kpu_c = blkdiag(Kpu_c, Kpu_c);
Kup_c = blkdiag(Kup_c, Kup_c);
```

最终组装为：

```matlab
AK = [-1/rho*Kpp_c, Kpu_c;
       Kup_c,        Kuu_c];
```

**公式 (10)**

$$[ \mathcal{K} = \int_{\Omega}\rho\,\boldsymbol{v}\cdot\nabla_{t}\boldsymbol{u} - \frac{1}{2}\rho\|\boldsymbol{v}\|^{2}\,\mathrm{d}\Omega ]$$

---

#### 公式 (11)：Hamilton 驻值原理

**最底层代码** — [`main.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/main.m)（第 31 行）

驻值条件 $\delta H = 0$ 最终离散化为线性方程组，直接求解：

```matlab
u = GK\f;
```

**公式 (11)**

$$[ H = H\left[\boldsymbol{v},\boldsymbol{u},\nabla^{\mathrm{sym}}\boldsymbol{u},\nabla_{t}\boldsymbol{u}\right]\to\mathrm{stat}\;\Leftrightarrow\;\delta H = \delta_{\boldsymbol{v}}H + \delta_{\boldsymbol{u}}H = 0\quad\forall\,\delta\boldsymbol{v},\delta\boldsymbol{u} ]$$

---

#### 公式 (12)：独立变分条件（两场弱形式）

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 91 行）

两场独立变分条件对应单元刚度矩阵的四块结构：

```matlab
AK = [-1/rho*Kpp_c, Kpu_c;
       Kup_c,        Kuu_c];
```

**公式 (12)**

$$[ \begin{cases}\delta_{\boldsymbol{v}}H = 0 & \forall\,\delta\boldsymbol{v} \\\delta_{\boldsymbol{u}}H = 0 & \forall\,\delta\boldsymbol{u}\end{cases} ]$$

---

#### 公式 (13)：驻值条件的展开式

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–91 行）

四个积分项分别对应 $\mathbf{K}_{vv}$、$\mathbf{K}_{vu}$、$\mathbf{K}_{uv}$、$\mathbf{K}_{uu}$ 的构造：

```matlab
% -ρv·δv 项 → Kpp_c
Kpp_c = [Int*NN(1,1), Int*NN(1,2);
         Int*NN(2,1), Int*NN(2,2)] + ...
         theta*h*[Int*dNN(1,1), Int*dNN(1,2);
                  Int*dNN(2,1), Int*dNN(2,2)];
% ρ∇_t u·δv 项 → Kpu_c
Kpu_c = [Int*NdN(1,1), Int*NdN(1,2);
         Int*NdN(2,1), Int*NdN(2,2)] + ...
    theta*h*[Int*dNdN(1,1), Int*dNdN(1,2);
             Int*dNdN(2,1), Int*dNdN(2,2)];
% ρv·∇_t δu 项 → Kup_c
Kup_c = [Int*NdN(1,1), Int*NdN(1,2);
         Int*NdN(2,1), Int*NdN(2,2)] + ...
    theta*h*[Int*dNdN(1,1), Int*dNdN(1,2);
             Int*dNdN(2,1), Int*dNdN(2,2)];
```

**公式 (13)**

$$[ \begin{cases}\delta_{\boldsymbol{v}}H = \int_{I}\int_{\Omega}\rho(\nabla_{t}\boldsymbol{u}-\boldsymbol{v})\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{v} \\\delta_{\boldsymbol{u}}H = \int_{I}\int_{\Omega}\rho\boldsymbol{v}\cdot\nabla_{t}\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t - \int_{I}\left(\int_{\Omega}\boldsymbol{\sigma}:\boldsymbol{\varepsilon}(\delta\boldsymbol{u})\,\mathrm{d}V - \cdots\right)\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{u}\end{cases} ]$$

---

#### 公式 (14)：分部积分后的驻值条件（弱形式）

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–91 行）+ [`getForce.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/getForce.m)（第 49–73 行）

时间方向分部积分后的弱形式，由单元刚度矩阵和力向量共同实现：

```matlab
% 单元刚度矩阵的完整构造（对应弱形式左端项）
AK = [-1/rho*Kpp_c, Kpu_c;
       Kup_c,        Kuu_c];

% 外力向量的时间积分（对应弱形式右端项）
for t = 1:sumT-1
    h = zline(t+1)-zline(t);
    T = [0;0]; intDf = [0;0];
    for m = 1:nip
        N = fun(x(m),0,0,1,2);
        J = h/2;
        T    = T    + w(m)*N*J;
        df   = dzshape(N'*[zline(t),zline(t+1)]',zline(t),zline(t+1));
        intDf= intDf + w(m)*df'*J;
    end
    P = [p*T(1);p*T(2)] + theta*h*[p*intDf(1);p*intDf(2)];
end
```

**公式 (14)**

$$[ \begin{cases}\int_{I}\int_{\Omega}-\rho\boldsymbol{v}\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t + \int_{I}\int_{\Omega}\rho\nabla_{t}\boldsymbol{u}\cdot\delta\boldsymbol{v}\,\mathrm{d}V\,\mathrm{d}t = 0 & \forall\,\delta\boldsymbol{v} \\\int_{I}\int_{\Omega}\rho\boldsymbol{v}\cdot\nabla_{t}\delta\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t + \int_{I}\int_{\Omega}\boldsymbol{\sigma}:\delta\nabla^{\mathrm{sym}}\boldsymbol{u}\,\mathrm{d}V\,\mathrm{d}t = \int_{I}\left(\int_{\Omega}\tilde{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}V + \int_{\Gamma_{\sigma}}\boldsymbol{t}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t & \forall\,\delta\boldsymbol{u}\end{cases} ]$$

---

### 2.2 时空双线性格式与时间迎风稳定化

---

#### 公式 (15)：双线性形式的分块结构

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 91 行）

单元刚度矩阵正是公式 (15) 给出的四块双线性形式的离散矩阵实现：

```matlab
AK = [-1/rho*Kpp_c, Kpu_c;
       Kup_c,        Kuu_c];
```

**公式 (15)**

$$[ A(\boldsymbol{v},\boldsymbol{u},\delta\boldsymbol{v},\delta\boldsymbol{u}) = \begin{pmatrix}A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v})+A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u})\\A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v})+A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u})\end{pmatrix} = \begin{pmatrix}\mathbf{0}\\l(\delta\boldsymbol{u})\end{pmatrix} ]$$

---

#### 公式 (16)：原始双线性形式的各分量

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–90 行）

四个双线性形式（不含迎风项，即 $\theta=0$ 时）：

```matlab
% A^trv(δv, v): -ρ δv·v
Kpp_c = [Int*NN(1,1), Int*NN(1,2);
         Int*NN(2,1), Int*NN(2,2)];

% A^trv(δv, u): ρ δv·∇_t u
Kpu_c = [Int*NdN(1,1), Int*NdN(1,2);
         Int*NdN(2,1), Int*NdN(2,2)];

% A^trv(δu, v): ρ δu·∇_t v
Kup_c = [Int*NdN(1,1), Int*NdN(1,2);
         Int*NdN(2,1), Int*NdN(2,2)];

% A^trv(δu, u): ε(δu):D:ε(u)
Kuu_c = [NN(1,1)*int_uu11, NN(2,1)*int_uu11, NN(1,1)*int_uu12, NN(2,1)*int_uu12;
         NN(1,2)*int_uu11, NN(2,2)*int_uu11, NN(1,2)*int_uu12, NN(2,2)*int_uu12;
         NN(1,1)*int_uu21, NN(2,1)*int_uu21, NN(1,1)*int_uu22, NN(2,1)*int_uu22;
         NN(1,2)*int_uu21, NN(2,2)*int_uu21, NN(1,2)*int_uu22, NN(2,2)*int_uu22];
```

**公式 (16)**

$$[ \begin{array}{l} A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{v}) = \int_{0}^{T}\int_{\Omega}-\rho\,\delta\boldsymbol{v}\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t, \\ A^{\mathrm{trv}}(\delta\boldsymbol{v},\boldsymbol{u}) = \int_{0}^{T}\int_{\Omega}\rho\,\delta\boldsymbol{v}\cdot\nabla_{t}\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t, \\ A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{v}) = \int_{0}^{T}\int_{\Omega}\rho\,\delta\boldsymbol{u}\cdot\nabla_{t}\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t, \\ A^{\mathrm{trv}}(\delta\boldsymbol{u},\boldsymbol{u}) = \int_{0}^{T}\int_{\Omega}\boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega\,\mathrm{d}t. \end{array} ]$$

---

#### 公式 (17)：线性载荷形式 $l(\delta\boldsymbol{u})$（无迎风项）

**最底层代码** — [`getForce.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/getForce.m)（第 49–72 行）

外力向量的时间积分（对应 $\theta=0$ 的情形为第一项）：

```matlab
for t = 1:sumT-1
    h = zline(t+1)-zline(t);
    T = [0;0];
    for m = 1:nip
        N = fun(x(m),0,0,1,2);
        J = h/2;
        T = T + w(m)*N*J;
    end
    P = [p*T(1);p*T(2)];   % θ=0 时的外力项
    ff(faceNodeIDNew+sumNodeNew) = ff(faceNodeIDNew+sumNodeNew) + P;
end
```

**公式 (17)**

$$[ l(\delta\boldsymbol{u}) = \int_{0}^{T}\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Omega + \int_{\Gamma_{\sigma}}\boldsymbol{t}\cdot\delta\boldsymbol{u}\,\mathrm{d}\Gamma\right)\mathrm{d}t ]$$

---

#### 公式 (18)：含时间迎风稳定化的双线性形式

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–91 行）

迎风项通过 `theta*h*[...]` 加到各块矩阵中，下面展示含 $\theta h$ 稳定项的完整构造：

```matlab
% A_vv：-ρ(δv + θh∇_t δv)·v
Kpp_c = [Int*NN(1,1),  Int*NN(1,2);
         Int*NN(2,1),  Int*NN(2,2)] + ...
    theta*h*[Int*dNN(1,1), Int*dNN(1,2);
             Int*dNN(2,1), Int*dNN(2,2)];

% A_vu：ρ(δv + θh∇_t δv)·∇_t u
Kpu_c = [Int*NdN(1,1),  Int*NdN(1,2);
         Int*NdN(2,1),  Int*NdN(2,2)] + ...
    theta*h*[Int*dNdN(1,1), Int*dNdN(1,2);
             Int*dNdN(2,1), Int*dNdN(2,2)];

% A_uv：ρ(δu + θh∇_t δu)·∇_t v
Kup_c = [Int*NdN(1,1),  Int*NdN(1,2);
         Int*NdN(2,1),  Int*NdN(2,2)] + ...
    theta*h*[Int*dNdN(1,1), Int*dNdN(1,2);
             Int*dNdN(2,1), Int*dNdN(2,2)];

% A_uu：ε(δu + θh∇_t δu):D:ε(u)
Kuu_c = [NN(1,1)*int_uu11, NN(2,1)*int_uu11, ...] + ...
    theta*h*[dNN(1,1)*int_uu11, dNN(1,2)*int_uu11, ...];
```

**公式 (18)**

$$[ \begin{aligned} A_{\boldsymbol{vv}}(\delta\boldsymbol{v},\boldsymbol{v}) &= \int_{0}^{T}\int_{\Omega}-\rho(\delta\boldsymbol{v}+\theta h\nabla_{t}\delta\boldsymbol{v})\cdot\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t, \\ A_{\boldsymbol{vu}}(\delta\boldsymbol{v},\boldsymbol{u}) &= \int_{0}^{T}\int_{\Omega}\rho(\delta\boldsymbol{v}+\theta h\nabla_{t}\delta\boldsymbol{v})\cdot\nabla_{t}\boldsymbol{u}\,\mathrm{d}\Omega\,\mathrm{d}t, \\ A_{\boldsymbol{uv}}(\delta\boldsymbol{u},\boldsymbol{v}) &= \int_{0}^{T}\int_{\Omega}\rho(\delta\boldsymbol{u}+\theta h\nabla_{t}\delta\boldsymbol{u})\cdot\nabla_{t}\boldsymbol{v}\,\mathrm{d}\Omega\,\mathrm{d}t, \\ A_{\boldsymbol{uu}}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_{0}^{T}\int_{\Omega}\boldsymbol{\varepsilon}(\delta\boldsymbol{u}+\theta h\nabla_{t}\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega\,\mathrm{d}t. \end{aligned} ]$$

---

#### 公式 (19)：含迎风稳定化的线性载荷形式

**最底层代码** — [`getForce.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/getForce.m)（第 49–73 行）

加入迎风项 $\theta h\nabla_t(\delta\boldsymbol{u})$ 后，外力向量中同时包含形函数积分 `T` 和导数积分 `intDf` 两项：

```matlab
for t = 1:sumT-1
    h = zline(t+1)-zline(t);
    T = [0;0];  intDf = [0;0];
    for m = 1:nip
        N  = fun(x(m),0,0,1,2);          % φ_t(ξ)
        J  = h/2;
        T  = T  + w(m)*N*J;              % ∫φ_t dt
        df = dzshape(N'*[zline(t),zline(t+1)]', zline(t), zline(t+1));
        intDf = intDf + w(m)*df'*J;      % ∫∇_t φ_t dt
    end
    P = [p*T(1);p*T(2)] + theta*h*[p*intDf(1);p*intDf(2)];
end
```

**公式 (19)**

$$[ l(\delta\boldsymbol{u}) = \int_{0}^{T}\left(\int_{\Omega}\bar{\boldsymbol{f}}\cdot(\delta\boldsymbol{u}+\theta h\nabla_{t}\delta\boldsymbol{u})\,\mathrm{d}\Omega + \int_{\Gamma_{\sigma}}\boldsymbol{t}\cdot(\delta\boldsymbol{u}+\theta h\nabla_{t}\delta\boldsymbol{u})\,\mathrm{d}\Gamma\right)\mathrm{d}t ]$$

---

## 3. 空时虚元法（VEM for Space–Time Dynamics）

### 3.1 网格假设

本节描述了时空网格的构造原则，无独立编号的计算公式，但网格几何量（面积、重心、直径）的计算对应以下代码。

**最底层代码** — [`auxgeometry.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/auxgeometry.m)（第 13–22 行）

```matlab
for iel = 1:NT
    index = elem{iel};
    verts = node(index,:); verts1 = circshift(verts,-1);
    area_components = verts(:,1).*verts1(:,2) - verts1(:,1).*verts(:,2);
    ar = 0.5*abs(sum(area_components));          % |E|：单元面积
    area(iel) = ar;
    centroid(s,:) = sum((verts+verts1).*repmat(area_components,1,2))/(6*ar);  % x_E：重心
    diameter(s)   = max(pdist(verts));           % h_E：直径
    s = s+1;
end
```

时空网格（空间多边形 $\times$ 时间线段）由以下函数生成：

**最底层代码** — [`PolyMesh3Simple.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/PolyMesh3Simple.m)（第 23–37 行）

```matlab
node3 = [repmat(node,J+1,1), kron(z(:),ones(N,1))];
for level = 1:J
    for iel = 1:NT
        index = elem{iel} + (level-1)*N;  Nv = length(index);
        elemf{1} = index;               % 底面
        elemf{2} = index(end:-1:1)+N;   % 顶面
        for i = 1:Nv
            elemf{i+2} = index([v2(i),v1(i),v1(i),v2(i)]) + [0,0,N,N]; % 侧面
        end
        elem3{iel+(level-1)*NT} = elemf;
    end
end
```

---

### 3.2 投影算子

---

#### 公式 (20)：$H^1$ 投影算子的定义

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 14–30 行）

$H^1$ 投影 $\Pi_{\nabla}^{k,E}$ 由一致性矩阵 $G$ 和 $B$ 求解得到：

```matlab
% 计算 D 矩阵（在节点和重心处计算缩放单项式 m_k）
D = myM([x,y], k, hK, centroid(elemID,:), ndim);

% 计算 B 矩阵（梯度一致性条件）
Gradm  = [0 0; 1./hK*[1,0]; 1./hK*[0,1]];     % ∇m_k，k=1
rotid1 = [Nv,1:Nv-1];  rotid2 = [2:Nv,1];
normVec = 0.5*[y(rotid2)-y(rotid1), x(rotid1)-x(rotid2)]';  % 法向量
B = Gradm * normVec;    % 一致性矩阵 B

% 约束条件（固定常数项）
Bs = B;  Bs(1,:) = 1/Nv;

% 一致性方程 G·Pi = B，求解 Pis = Pi^* 
G  = B*D;  Gs = Bs*D;
Pis = Gs\Bs;    % Π_∇^{k,E*}（系数矩阵）
Pi  = D*Pis;    % Π_∇^{k,E}（在节点基函数空间中的投影矩阵）
```

**公式 (20)**

$$[ a_{E}(w - \Pi_{\nabla}^{k,E}w,\,q_{k}) = 0 \quad \forall\,q_{k}\in P_{k}(E) ]$$

其中 $[ a_E(p,q) = \int_E \nabla p\cdot\nabla q\,\mathrm{d}\Omega ]$，$[ k=1 ]$。

---

#### 公式 (21)：投影算子的附加约束条件

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 24 行）

对 $k=1$，用各顶点均值替换 $B$ 的第一行来固定常数项：

```matlab
Bs = B;
Bs(1,:) = 1/Nv;   % k=1 时的约束：(1/N_V^E) Σ(Π w - w) = 0
```

**公式 (21)**

$$[ \begin{cases}\dfrac{1}{N_{V}^{E}}\displaystyle\sum_{i=1}^{N_{V}^{E}}\left(\Pi_{\nabla}^{k,E}w - w\right) = 0, & k=1, \\[6pt] \dfrac{1}{|E|}\displaystyle\int_{E}\left(\Pi_{\nabla}^{k,E}w - w\right)\mathrm{d}\Omega = 0, & k>1. \end{cases} ]$$

---

#### 公式 (22)：虚元空间的定义 $\mathcal{V}_h^E$

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（整个函数）

虚元空间 $\mathcal{V}_h^E$ 的自由度（顶点处的值）隐含于投影算子的计算结构中，节点值即为 DOF：

```matlab
index = elem{elemID};          % 单元顶点全局编号
Nv    = length(index);         % 顶点数 N_V^E
x     = node(index,1);
y     = node(index,2);
```

**公式 (22)**

$$[ \mathcal{V}_{h}^{E} \coloneqq \{v\in H^{1}:\;v|_{\partial E}\in\mathcal{B}(\partial E),\;\Delta v\in(P_{k-2}(E))^{3}\} ]$$

---

#### 公式 (23)：边界元空间 $\mathcal{B}(\partial E)$

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 18–22 行）

各边上的形函数为线性多项式（$k=1$），通过边的端点节点编号和法向量来处理边界积分：

```matlab
rotid1 = [Nv,1:Nv-1];   rotid2 = [2:Nv,1];
normVec = 0.5*[y(rotid2)-y(rotid1), x(rotid1)-x(rotid2)]';
B = Gradm * normVec;     % 每条边上 ∇m_k 与法向量的乘积
```

**公式 (23)**

$$[ \mathcal{B}(\partial E) \coloneqq \{v\in C^{0}(\partial E):\;v_{F}\in P_{k}(F),\;\forall F\in\partial E\} ]$$

---

#### 公式 (24)：$v_h$ 及其投影在不同空间中的展开

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 29–30 行）+ [`myM.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/myM.m)（第 13–14 行）

缩放单项式基函数 $\boldsymbol{m}_k$（k=1 时为 $[1,\,(x-x_E)/h_E,\,(y-y_E)/h_E]$）：

```matlab
% myM.m：计算缩放单项式 m_k（在节点处）
M = [1.0+0*x,  (x-xK)./hK,  (y-yK)./hK];   % k=1

% calculatePi.m：用 m_k 在节点处的值组成 D，然后计算投影矩阵
D    = myM([x,y], k, hK, centroid(elemID,:), ndim);  % D = m_k(节点)
Pis  = Gs\Bs;    % Π_∇^{k,E*}：从 DOF 到多项式系数的映射
Pi   = D*Pis;    % Π_∇^{k,E}：在 VEM 节点基函数空间中的投影矩阵
```

**公式 (24)**

$$[ v_{h} = \boldsymbol{\phi}^{T}\hat{v}_{h}, \quad \Pi_{\nabla}^{k,E}v_{h} = \boldsymbol{m}_{k}^{T}\Pi_{\nabla}^{k,E}\hat{v}_{h} = \boldsymbol{\phi}^{T}\boldsymbol{\Pi}_{\nabla}^{k,E}\hat{v}_{h} ]$$

其中 $\boldsymbol{\phi}$ 为 VEM 形函数，$\boldsymbol{m}_k$ 为多项式空间基函数，$\boldsymbol{\Pi}_{\nabla}^{k,E}$ 为投影矩阵。

---

#### 公式 (25)：$L^2$ 正交投影算子的定义

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 21–35 行）+ [`polyBisis.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/polyBisis.m)（第 8–9 行）

对 $k=1$，$\Pi_{k,E}^{0} = \Pi_{\nabla}^{k,E}$，故在代码中公用同一组 `Pi`、`Pis`。$L^2$ 投影通过在多边形三角剖分上的 Gauss 积分计算：

```matlab
[lambda,weight] = quadpts(2);          % 三角形高斯积分点
Int = zeros(Nv);
for iel = 1:NT
    vT   = nodeT(elemT(iel,:),:);
    area = 0.5*abs(det([[1;1;1],vT]));
    xy   = lambda*vT;                  % 积分点坐标
    for p = 1:size(xy,1)
        M   = polyBisis(xy(p,:), hK, centroid, 1, 2);  % m_k(积分点)
        Pim = M*Pis;                   % Π^0 v_h(积分点) = m_k^T Π^{0*} v̂_h
        Int = Int + area*weight(p)*(Pim'*Pim);  % ∫(Π^0 v_h)^2 dΩ
    end
end
```

**公式 (25)**

$$[ \left(v-\Pi_{k,E}^{0}v,\,q_{k}\right)_{E} = 0, \quad \forall\,q_{k}\in P_{k}(E) ]$$

---

### 3.3 空间域中的离散双线性格式

---

#### 公式 (27)：近似双线性形式 $a_h^E$ 和 $m_h^E$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 21–35 行、第 68–78 行）

两个近似双线性形式（含稳定项）：

```matlab
% --- m_h^E(p,q) = K_0 的计算（第 21-35 行）---
Int = zeros(Nv);
for iel = 1:NT
    ...
    for p = 1:size(xy,1)
        M   = polyBisis(xy(p,:), hK, centroid, 1, 2);
        Pim = M*Pis;
        Int = Int + area*weight(p)*(Pim'*Pim);   % K_c^0
    end
end
I   = eye(size(Pi,1));
Int = Int + Area*(I-Pi)'*(I-Pi);                 % K_c^0 + K_s^0 = K_0

% --- a_h^E(δu,u) = K_ε 的计算（第 68-78 行）---
dM1    = blkdiag(dM', dM');
G0     = Area*dM1'*A'*D*A*dM1;                  % ∫∇M A^T D̃ A ∇M^T dΩ（单点）
int_uu_c = Pis'*G0*Pis;                         % K_c^ε
int_uu   = int_uu_c + 0.15*trace(int_uu_c)*(eye(size(Pi,1))-Pi)'*(eye(size(Pi,1))-Pi);  % K_ε
```

**公式 (27)**

$$[ \begin{aligned} a_{h}^{E}(p_{h},q_{h}) &\coloneqq a^{E}(\Pi_{\nabla}^{k,E}p_{h},\Pi_{\nabla}^{k,E}q_{h}) + S^{E}\!\left((\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{p}_{h},(\mathbf{I}-\Pi_{\nabla}^{k,E})\hat{q}_{h}\right), \\ m_{h}^{E}(p_{h},q_{h}) &\coloneqq m^{E}(\Pi_{k,E}^{0}p_{h},\Pi_{k,E}^{0}q_{h}) + R^{E}\!\left((\mathbf{I}-\Pi_{k,E}^{0})\hat{p}_{h},(\mathbf{I}-\Pi_{k,E}^{0})\hat{q}_{h}\right). \end{aligned} ]$$

---

#### 公式 (28)：$a_h^E$ 的矩阵表达式与 $\mathbf{K}_\nabla$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 68–78 行）+ [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 29–30 行）

```matlab
% calculatePi.m：计算 Π_∇^{k,E*} 和 Π_∇^{k,E}
Pis = Gs\Bs;   % Π_∇^{k,E*}（系数形式）
Pi  = D*Pis;   % Π_∇^{k,E}

% elemK.m：计算 K_c^∇ + K_s^∇ = K_∇
dM      = myGradmc(centroid, 1, hK, centroid, 2);  % ∇m_k（常数，k=1）
dM1     = blkdiag(dM', dM');
G0      = Area*dM1'*A'*D*A*dM1;    % (Π^{∇*})^T ∫∇m·∇m^T dΩ Π^{∇*}（近似，单点）
int_uu_c = Pis'*G0*Pis;             % K_c^∇（位移刚度一致性项）
int_uu   = int_uu_c + ...
    0.15*trace(int_uu_c)*(eye(size(Pi,1))-Pi)'*(eye(size(Pi,1))-Pi); % K_∇ = K_c^∇+K_s^∇
```

**公式 (28)**

$$[ \begin{aligned} a_{h}^{E}(p,q) &= \hat{p}_{h}^{T}\!\left[(\boldsymbol{\Pi}_{\nabla}^{k,E*})^{T}\!\int_{\mathcal{K}_{x}}\!\nabla\boldsymbol{m}_{k}\nabla\boldsymbol{m}_{k}^{T}\,\mathrm{d}\Omega\,\boldsymbol{\Pi}_{\nabla}^{k,E*} + (\mathbf{I}-\boldsymbol{\Pi}_{\nabla}^{k,E})^{T}(\mathbf{I}-\boldsymbol{\Pi}_{\nabla}^{k,E})\right]\hat{q}_{h} \\ &= \hat{p}_{h}^{T}(\mathbf{K}_{c}^{\nabla}+\mathbf{K}_{s}^{\nabla})\hat{q}_{h} = \hat{p}_{h}^{T}\mathbf{K}_{\nabla}\hat{q}_{h}. \end{aligned} ]$$

---

#### 公式 (29)：$m_h^E$ 的矩阵表达式与 $\mathbf{K}_0$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 21–35 行）

```matlab
Int = zeros(Nv);
[lambda,weight] = quadpts(2);     % 三角形积分点
NT  = size(elemT,1);
for iel = 1:NT
    vT   = nodeT(elemT(iel,:),:);
    area = 0.5*abs(det([[1;1;1],vT]));
    xy   = lambda*vT;
    for p = 1:size(xy,1)
        M   = polyBisis(xy(p,:), hK, centroid, 1, 2);  % m_k(积分点)
        Pim = M*Pis;          % (Π^{0*})^T m_k(点)，即 Π^0 v_h 在积分点的值
        Int = Int + area*weight(p)*(Pim'*Pim);  % K_c^0（一致性项）
    end
end
I   = eye(size(Pi,1));
Int = Int + Area*(I-Pi)'*(I-Pi);  % K_0 = K_c^0 + K_s^0（加稳定项）
```

**公式 (29)**

$$[ \begin{aligned} m_{h}^{E}(p,q) &= \hat{p}_{h}^{T}\!\left[(\boldsymbol{\Pi}_{k,E}^{0*})^{T}\!\int_{\mathcal{K}_{x}}\!\boldsymbol{m}_{k}\boldsymbol{m}_{k}^{T}\,\mathrm{d}\Omega\,\boldsymbol{\Pi}_{k,E}^{0*} + |E|(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^{0})^{T}(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^{0})\right]\hat{q}_{h} \\ &= \hat{p}_{h}^{T}(\mathbf{K}_{c}^{0}+\mathbf{K}_{s}^{0})\hat{q}_{h} = \hat{p}_{h}^{T}\mathbf{K}_{0}\hat{q}_{h}. \end{aligned} ]$$

---

#### 公式 (30)：$\mathbf{K}_c^0$、$\mathbf{K}_s^0$、$\mathbf{K}_0$ 的定义

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 21–35 行）

代码中 `Int` 变量即为 $\mathbf{K}_0$：

```matlab
% K_c^0（一致性项）：三角形积分累加
for iel = 1:NT
    ...
    for p = 1:size(xy,1)
        Pim = M*Pis;
        Int = Int + area*weight(p)*(Pim'*Pim);   % ← K_c^0
    end
end

% K_s^0（稳定项）：
Int = Int + Area*(I-Pi)'*(I-Pi);   % K_0 = K_c^0 + K_s^0
```

**公式 (30)**

$$[ \begin{aligned} \mathbf{K}_{c}^{0} &\coloneqq (\boldsymbol{\Pi}_{k,E}^{0*})^{T}\!\int_{\mathcal{K}_{x}}\!\boldsymbol{m}_{k}\boldsymbol{m}_{k}^{T}\,\mathrm{d}\Omega\,\boldsymbol{\Pi}_{k,E}^{0*}, \\ \mathbf{K}_{s}^{0} &\coloneqq |E|(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^{0})^{T}(\mathbf{I}-\boldsymbol{\Pi}_{k,E}^{0}), \\ \mathbf{K}_{0} &\coloneqq \mathbf{K}_{c}^{0}+\mathbf{K}_{s}^{0}. \end{aligned} ]$$

---

#### 公式 (31)：$\mathbf{K}_c^\nabla$、$\mathbf{K}_s^\nabla$、$\mathbf{K}_\nabla$ 的定义

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 14–30 行）+ [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 68–78 行）+ [`myGradmc.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/myGradmc.m)（第 13–14 行）

```matlab
% myGradmc.m：计算 ∇m_k（k=1 时为常数矩阵）
dM = [0,0; 1./hK*[1,0]; 1./hK*[0,1]];   % ∇m_1, ∇m_2, ∇m_3（相对于物理坐标）

% elemK.m：计算 K_c^∇（位移刚度一致性项）
dM     = myGradmc(centroid, 1, hK, centroid, 2);
dM1    = blkdiag(dM', dM');           % 2D 向量位移梯度矩阵
G0     = Area*dM1'*A'*D*A*dM1;       % ≈ ∫ ∇M A^T D̃ A ∇M^T dΩ
int_uu_c = Pis'*G0*Pis;              % K_c^∇ = (Π^{∇*})^T G0 Π^{∇*}

% K_s^∇（稳定项）
int_uu   = int_uu_c + 0.15*trace(int_uu_c)*(eye(size(Pi,1))-Pi)'*(eye(size(Pi,1))-Pi);
% int_uu = K_∇ = K_c^∇ + K_s^∇
```

**公式 (31)**

$$[ \begin{aligned} \mathbf{K}_{c}^{\nabla} &\coloneqq (\boldsymbol{\Pi}_{\nabla}^{k,E*})^{T}\!\int_{\mathcal{K}_{x}}\!\nabla\boldsymbol{m}\,\nabla\boldsymbol{m}^{T}\,\mathrm{d}\Omega\,\boldsymbol{\Pi}_{\nabla}^{k,E*}, \\ \mathbf{K}_{s}^{\nabla} &\coloneqq (\mathbf{I}-\boldsymbol{\Pi}_{\nabla}^{k,E})^{T}(\mathbf{I}-\boldsymbol{\Pi}_{\nabla}^{k,E}), \\ \mathbf{K}_{\nabla} &\coloneqq \mathbf{K}_{c}^{\nabla}+\mathbf{K}_{s}^{\nabla}. \end{aligned} ]$$

---

#### 公式 (32)：位移向量的分量表示

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 64–66 行）

x 和 y 分量分别处理，然后用 `blkdiag` 组装：

```matlab
Kpp_c = blkdiag(Kpp_c, Kpp_c);   % 对应 [u_x; u_y] 的 x/y 块
Kpu_c = blkdiag(Kpu_c, Kpu_c);
Kup_c = blkdiag(Kup_c, Kup_c);
```

**公式 (32)**

$$[ \boldsymbol{u} = [u_{x},\,u_{y}]^{T}, \quad \delta\boldsymbol{u} = [\delta u_{x},\,\delta u_{y}]^{T} ]$$

---

#### 公式 (33)：速度场双线性形式 $m_h^E(\delta\boldsymbol{v},\boldsymbol{v})$ 与 $\mathbb{K}_0$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 52–66 行）

```matlab
% 组装 Kpp_c（对应 -ρ∫δv·v dΩ，含时间积分 NN）
Kpp_c = [Int*NN(1,1), Int*NN(1,2);
         Int*NN(2,1), Int*NN(2,2)] + ...
         theta*h*[Int*dNN(1,1), Int*dNN(1,2);
                  Int*dNN(2,1), Int*dNN(2,2)];
% 扩展到二维（x/y 分量）：K_0^{I,I} 和 K_0^{II,II} 构成 K_0 的对角块
Kpp_c = blkdiag(Kpp_c, Kpp_c);   % = diag(L⊗K_0^{I,I} + θhQ⊗K_0^{I,I}, ...)
```

**公式 (33)**

$$[ m_{h}^{E}(\delta\boldsymbol{v},\boldsymbol{v}) = \begin{bmatrix}\delta\hat{v}_{x}^{T} & \delta\hat{v}_{y}^{T}\end{bmatrix}\begin{bmatrix}\mathbf{K}_{0} & \\ & \mathbf{K}_{0}\end{bmatrix}\begin{bmatrix}\hat{v}_{x}\\\hat{v}_{y}\end{bmatrix} = \delta\hat{\boldsymbol{v}}^{T}\mathbb{K}_{0}\hat{\boldsymbol{v}}, \quad \mathbb{K}_{0}\coloneqq\begin{bmatrix}\mathbf{K}_{0} & \\ & \mathbf{K}_{0}\end{bmatrix} ]$$

---

#### 公式 (34)：应变的 Voigt 表示

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 69–73 行）

```matlab
A1 = [1,0; 0,0; 0,1];
A2 = [0,0; 0,1; 1,0];
A  = [A1, A2];            % A：3×4 矩阵，将 d(u) 映射到 Voigt 应变
```

**公式 (34)**

$$[ \tilde{\boldsymbol{\varepsilon}}(\boldsymbol{u}) = \mathbf{A}\,\boldsymbol{d}(\boldsymbol{u}) ]$$

---

#### 公式 (35)：矩阵 $\mathbf{A}$ 和位移梯度向量 $\boldsymbol{d}(\boldsymbol{u})$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 69–72 行）

```matlab
A1 = [1,0; 0,0; 0,1];    % ∂u_x/∂x → ε_xx, ∂u_y/∂x → ε_xy (x 列)
A2 = [0,0; 0,1; 1,0];    % ∂u_x/∂y → ε_xy, ∂u_y/∂y → ε_yy (y 列)
A  = [A1, A2];            % A = [A1, A2]，3×4
```

**公式 (35)**

$$[ \mathbf{A} = \begin{bmatrix}1&0&0&0\\0&0&0&1\\0&1&1&0\end{bmatrix}, \quad \boldsymbol{d}(\boldsymbol{u}) = \begin{bmatrix}\frac{\partial u_{x}}{\partial x}&\frac{\partial u_{x}}{\partial y}&\frac{\partial u_{y}}{\partial x}&\frac{\partial u_{y}}{\partial y}\end{bmatrix}^{T} ]$$

---

#### 公式 (36)：位移向量的投影 $\Pi_\nabla^{k,E}\boldsymbol{v}_h$

**最底层代码** — [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m)（第 29–30 行）+ [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 74–76 行）

```matlab
% calculatePi.m：单分量投影
Pis = Gs\Bs;   % Π_∇^{k,E*}（单分量）
Pi  = D*Pis;   % Π_∇^{k,E}

% elemK.m：扩展到二维向量场（blkdiag 实现 diag(Π, Π)）
Pis = blkdiag(Pis, Pis);   % Π^* 的向量场版本
Pi  = blkdiag(Pi,  Pi);
```

**公式 (36)**

$$[ \Pi_{\nabla}^{k,E}\boldsymbol{v}_{h} = \begin{bmatrix}\boldsymbol{m}^{T}&\\&\boldsymbol{m}^{T}\end{bmatrix}\begin{bmatrix}\boldsymbol{\Pi}_{\nabla}^{k,E*}&\\&\boldsymbol{\Pi}_{\nabla}^{k,E*}\end{bmatrix}\begin{bmatrix}\hat{u}_{h}^{x}\\\hat{u}_{h}^{y}\end{bmatrix} = \mathbf{M}^{T}\boldsymbol{\Pi}_{\nabla}^{*}\hat{\boldsymbol{u}}_{h} ]$$

---

#### 公式 (37)：应变能双线性形式 $a_h^E(\delta\boldsymbol{u},\boldsymbol{u})$ 与 $\mathbf{K}_\varepsilon$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 68–90 行）

```matlab
dM      = myGradmc(centroid, 1, hK, centroid, 2);  % ∇m_k（常数矩阵）
A1 = [1,0;0,0;0,1]; A2 = [0,0;0,1;1,0]; A = [A1,A2];
dM1     = blkdiag(dM', dM');              % 向量场梯度 ∇M = diag(∇m, ∇m)^T
G0      = Area * dM1'*A'*D*A*dM1;        % K_c^ε（一致性项，单点积分近似）
Pis     = blkdiag(Pis, Pis);             % 向量场投影矩阵 Π^*
Pi      = blkdiag(Pi,  Pi);
int_uu_c = Pis'*G0*Pis;                  % (Π^*)^T ∫∇M A^T D̃ A ∇M^T dΩ Π^*

% 稳定参数 γ = 0.15 × tr(K_c^ε) / (注：代码中固定取 0.15)
int_uu = int_uu_c + 0.15*trace(int_uu_c)*(eye(size(Pi,1))-Pi)'*(eye(size(Pi,1))-Pi);
% int_uu = K_ε = K_c^ε + γ K_s^ε
```

**公式 (37)**

$$[ \begin{aligned} a_{h}^{E}(\delta\boldsymbol{u},\boldsymbol{u}) &= \int_{\mathcal{K}_{x}}\boldsymbol{\varepsilon}(\delta\boldsymbol{u}):\mathbb{D}:\boldsymbol{\varepsilon}(\boldsymbol{u})\,\mathrm{d}\Omega \\ &= \delta\hat{\boldsymbol{u}}^{T}\!\left((\boldsymbol{\Pi}_{\nabla}^{*})^{T}\!\int_{\mathcal{K}_{x}}\!\nabla\mathbf{M}\mathbf{A}^{T}\tilde{\mathbb{D}}\mathbf{A}\nabla\mathbf{M}^{T}\,\mathrm{d}\Omega\,\boldsymbol{\Pi}_{\nabla}^{*} + \gamma\begin{bmatrix}\mathbf{K}_{s}^{\nabla}&\\&\mathbf{K}_{s}^{\nabla}\end{bmatrix}\right)\hat{\boldsymbol{u}} \\ &= \delta\hat{\boldsymbol{u}}^{T}(\mathbf{K}_{c}^{\varepsilon}+\gamma\mathbf{K}_{s}^{\varepsilon})\hat{\boldsymbol{u}} = \delta\hat{\boldsymbol{u}}^{T}\mathbf{K}_{\varepsilon}\hat{\boldsymbol{u}}. \end{aligned} ]$$

其中 $[ \gamma = \frac{1}{4N_K}\mathrm{tr}(\mathbf{K}_c^\varepsilon) ]$（代码中取 $\gamma = 0.15\cdot\mathrm{tr}(\mathbf{K}_c^\varepsilon)$）。

---

### 3.4 线性空时弹性的矩阵形式

---

#### 公式 (38)：时空元形函数 $\Phi_\kappa$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 41–50 行）

时空形函数为空间形函数和时间形函数的张量积，通过分别计算后用 Kronecker 积组合：

```matlab
for n = 1:nip
    f  = fun(point(n),0,0,1,2);             % φ_{κ_t}(ξ)（时间方向形函数）
    df = dzshape(f'*zline', zline(1), zline(2));  % ∇_t φ_{κ_t}
    NN   = NN   + weight(n)*(f*f')  * (zline(2)-zline(1))/2;  % ∫φ_t φ_t^T dt
    NdN  = NdN  + weight(n)*(f*df') * (zline(2)-zline(1))/2;  % ∫φ_t ∇_t φ_t^T dt（注：df为行向量）
    dNN  = dNN  + weight(n)*(df'*f')* (zline(2)-zline(1))/2;  % ∫∇_t φ_t φ_t^T dt
    dNdN = dNdN + weight(n)*(df'*df)* (zline(2)-zline(1))/2;  % ∫∇_t φ_t ∇_t φ_t^T dt
end
```

**公式 (38)**

$$[ \Phi_{\kappa} = \begin{bmatrix}\Phi_{\kappa_{x}^{b}}\Phi_{\kappa_{t}}(1)\\\Phi_{\kappa_{x}^{t}}\Phi_{\kappa_{t}}(2)\end{bmatrix} ]$$

其中 $[ \Phi_{\kappa_x^b} = \Phi_{\kappa_x^t} =: \Phi_{\kappa_x} ]$ 为 VEM 形函数，$[ \Phi_{\kappa_t} ]$ 为时间方向线元基函数。

---

#### 公式 (39)：时间方向线元形函数

**最底层代码** — [`fun.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/fun.m)（第 4–6 行）

```matlab
function N = fun(ks, yt, zita, ndim, mnode)
N = zeros(mnode,1);
if ndim == 1
    N(1) = (1-ks)/2;    % Φ_{κ_t}(1) = (1-ξ)/2
    N(2) = (1+ks)/2;    % Φ_{κ_t}(2) = (1+ξ)/2
end
```

**公式 (39)**

$$[ \Phi_{\kappa_{t}}(1) = \frac{1-\xi}{2}, \quad \Phi_{\kappa_{t}}(2) = \frac{1+\xi}{2} ]$$

---

#### 公式 (40)：时空形函数的梯度

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 44–50 行）中的内部函数 `dzshape`（第 99–104 行）

```matlab
% 时间梯度 ∇_t Φ_{κ_t}（物理坐标）
function df = dzshape(t, t1, t2)
df1 = -1/(t2-t1);   % ∇_t Φ_{κ_t}(1) = -1/h
df2 =  1/(t2-t1);   % ∇_t Φ_{κ_t}(2) = +1/h
df = [df1, df2];

% 在 elemK.m 第 45 行调用：
df = dzshape(f'*zline', zline(1), zline(2));  % 得到 [−1/h, 1/h]
```

**公式 (40)**

$$[ \nabla_{x}\Phi_{\kappa} = \begin{bmatrix}\nabla_{x}\Phi_{\kappa_{x}^{b}}\Phi_{\kappa_{t}}(1)\\\nabla_{x}\Phi_{\kappa_{x}^{t}}\Phi_{\kappa_{t}}(2)\end{bmatrix}, \quad \nabla_{t}\Phi_{\kappa} = \begin{bmatrix}\Phi_{\kappa_{x}^{b}}\nabla_{t}\Phi_{\kappa_{t}}(1)\\\Phi_{\kappa_{x}^{t}}\nabla_{t}\Phi_{\kappa_{t}}(2)\end{bmatrix} ]$$

---

#### 公式 (41)：时空单元位移插值

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 41–50 行）+ [`globalK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/globalK.m)（第 41–43 行）

位移自由度在全局组装时按 $[\hat{u}_x^b, \hat{u}_x^t, \hat{u}_y^b, \hat{u}_y^t]$ 排列：

```matlab
% globalK.m：局部自由度到全局自由度的映射
index    = [elem{n}+sumNode*(m-1), elem{n}+sumNode*m];   % 底/顶面节点
Nv       = length(index);
elemDof  = [index, index+NsumNode, index+NsumNode*2, index+NsumNode*3];
```

**公式 (41)**

$$[ U_{h} = \begin{bmatrix}\Phi_{K}^{T}&\\&\Phi_{K}^{T}\end{bmatrix}\hat{U}_{h} = \boldsymbol{\Phi}_{K}^{T}\hat{U}_{h} ]$$

---

#### 公式 (42)：时空形函数矩阵 $\boldsymbol{\Phi}_K^T$ 和节点位移向量 $\hat{U}_h$

**最底层代码** — [`globalK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/globalK.m)（第 41–43 行）

```matlab
% 底面节点 + 顶面节点，再扩展到 x/y/vx/vy 四个字段
index    = [elem{n}+sumNode*(m-1), elem{n}+sumNode*m];
elemDof  = [index, index+NsumNode, index+NsumNode*2, index+NsumNode*3];
% 前两段 (index, index+NsumNode) 对应速度 v；
% 后两段 (index+NsumNode*2, index+NsumNode*3) 对应位移 u
```

**公式 (42)**

$$[ \boldsymbol{\Phi}_{K}^{T} \coloneqq \begin{bmatrix}\Phi_{K}^{T}&\\&\Phi_{K}^{T}\end{bmatrix}, \quad \hat{U}_{h} \coloneqq \begin{bmatrix}\hat{u}_{x}^{b}&\hat{u}_{x}^{t}&\hat{u}_{y}^{b}&\hat{u}_{y}^{t}\end{bmatrix}^{T} ]$$

---

#### 公式 (43)：时空单元刚度矩阵 $\mathbf{K}$ 的分块结构

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 91 行）

```matlab
AK = [-1/rho*Kpp_c,  Kpu_c;
       Kup_c,         Kuu_c];
```

**公式 (43)**

$$[ \mathbf{K} \coloneqq \begin{bmatrix}\mathbf{K}_{\boldsymbol{vv}} & \mathbf{K}_{\boldsymbol{vu}}\\\mathbf{K}_{\boldsymbol{uv}} & \mathbf{K}_{\boldsymbol{uu}}\end{bmatrix} ]$$

其中左上块（$\mathbf{K}_{vv}$）在代码中取负号并除以 $\rho$：`-1/rho * Kpp_c`。

---

#### 公式 (44)：$\mathbf{K}_{vv}$ 的积分展开（含 Kronecker 积结构）

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–66 行）

```matlab
% 时间积分矩阵 NN = L，dNN = Q（第 43-50 行）
for n = 1:nip
    f  = fun(point(n),0,0,1,2);
    df = dzshape(f'*zline', zline(1), zline(2));
    NN   = NN   + weight(n)*(f*f') *(zline(2)-zline(1))/2;   % L = ∫φ_t φ_t^T dt
    dNN  = dNN  + weight(n)*(df'*f')*(zline(2)-zline(1))/2;  % Q = ∫∇_t φ_t φ_t^T dt
end

% Kpp_c = -ρ^{-1}(L⊗K_0 + θh Q⊗K_0)
Kpp_c = [Int*NN(1,1),  Int*NN(1,2);
         Int*NN(2,1),  Int*NN(2,2)] + ...
         theta*h*[Int*dNN(1,1), Int*dNN(1,2);
                  Int*dNN(2,1), Int*dNN(2,2)];
Kpp_c = blkdiag(Kpp_c, Kpp_c);
```

（`Int` = $\mathbf{K}_0$，`NN` = $\mathbf{L}$，`dNN` = $\mathbf{Q}$，矩阵乘法即实现了 Kronecker 积结构。）

**公式 (44)**

$$[ \mathbf{K}_{\boldsymbol{vv}} = \rho^{-1}\begin{bmatrix}\int_{t_{n}}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_{t}}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}t\otimes\int_{\mathcal{K}_{x}}\boldsymbol{\phi}_{\mathcal{K}_{x}}\boldsymbol{\phi}_{\mathcal{K}_{x}}^{T}\,\mathrm{d}\Omega&\\&\cdots\end{bmatrix} + \rho^{-1}\theta h\begin{bmatrix}\int_{t_{n}}^{t_{n+1}}\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}t\otimes\int_{\mathcal{K}_{x}}\boldsymbol{\phi}_{\mathcal{K}_{x}}\boldsymbol{\phi}_{\mathcal{K}_{x}}^{T}\,\mathrm{d}\Omega&\\&\cdots\end{bmatrix} ]$$

---

#### 公式 (45)：空间积分 $\mathbf{K}_0$（Kronecker 积中的空间部分）

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 21–35 行）

（已在公式 (29)-(30) 中详细说明，此处指出 `Int` 即为 $\mathbf{K}_0$）：

```matlab
Int = zeros(Nv);
% ... 三角剖分积分 ...
Int = Int + Area*(I-Pi)'*(I-Pi);   % Int = K_c^0 + K_s^0 = K_0
```

**公式 (45)**

$$[ \int_{\mathcal{K}_{x}}\boldsymbol{\phi}_{\mathcal{K}_{x}}\boldsymbol{\phi}_{\mathcal{K}_{x}}^{T}\,\mathrm{d}\Omega = \mathbf{K}_{c}^{0}+\mathbf{K}_{s}^{0} =: \mathbf{K}_{0} ]$$

---

#### 公式 (46)：时间积分矩阵 $\mathbf{L}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–49 行）+ [`gaussInt.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/gaussInt.m)（第 11–12 行）

```matlab
nip = 2;
[point, weight] = gaussInt(nip);   % 2点 Gauss 积分，点 ≈ ±0.5774，权 = 1
NN = zeros(2);
for n = 1:nip
    f  = fun(point(n),0,0,1,2);           % φ_t(ξ)，ndim=1
    NN = NN + weight(n)*(f*f')*(zline(2)-zline(1))/2;  % L = h/2 Σ w_i φ_t(ξ_i) φ_t^T(ξ_i)
end
% NN 即为 L
```

**公式 (46)**

$$[ \int_{t_{n}}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_{t}}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}t = \frac{h}{2}\int_{-1}^{1}\boldsymbol{\phi}_{\mathcal{K}_{t}}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}\xi = \frac{h}{2}\sum_{i=1}^{2}w(i)\,\boldsymbol{\phi}_{\mathcal{K}_{t}}(\xi_{i})\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}(\xi_{i}) =: \mathbf{L} ]$$

---

#### 公式 (47)：时间积分矩阵 $\mathbf{Q}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–49 行）

```matlab
dNN = zeros(2);
for n = 1:nip
    f  = fun(point(n),0,0,1,2);
    df = dzshape(f'*zline', zline(1), zline(2));   % ∇_t φ_t = [-1/h, 1/h]
    dNN = dNN + weight(n)*(df'*f')*(zline(2)-zline(1))/2;  % Q = h/2 Σ w_i ∇_t φ_t(ξ_i) φ_t^T(ξ_i)
end
% dNN 即为 Q
```

**公式 (47)**

$$[ \int_{t_{n}}^{t_{n+1}}\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}t = \frac{h}{2}\sum_{i=1}^{2}w(i)\,\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}(\xi_{i})\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}(\xi_{i}) =: \mathbf{Q} ]$$

---

#### 公式 (48)：时间积分矩阵 $\mathbf{R}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–49 行）

```matlab
NdN = zeros(2);
for n = 1:nip
    f  = fun(point(n),0,0,1,2);
    df = dzshape(f'*zline', zline(1), zline(2));
    NdN = NdN + weight(n)*(f*df)*(zline(2)-zline(1))/2;   % R = h/2 Σ w_i φ_t(ξ_i) ∇_t φ_t^T(ξ_i)
end
% NdN 即为 R
```

**公式 (48)**

$$[ \int_{t_{n}}^{t_{n+1}}\boldsymbol{\phi}_{\mathcal{K}_{t}}\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}t = \frac{h}{2}\sum_{i=1}^{2}w(i)\,\boldsymbol{\phi}_{\mathcal{K}_{t}}(\xi_{i})\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}(\xi_{i}) =: \mathbf{R} ]$$

---

#### 公式 (49)：时间积分矩阵 $\mathbf{W}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 37–49 行）

```matlab
dNdN = zeros(2);
for n = 1:nip
    f  = fun(point(n),0,0,1,2);
    df = dzshape(f'*zline', zline(1), zline(2));
    dNdN = dNdN + weight(n)*(df'*df)*(zline(2)-zline(1))/2;  % W = h/2 Σ w_i ∇_t φ_t ∇_t φ_t^T
end
% dNdN 即为 W
```

**公式 (49)**

$$[ \int_{t_{n}}^{t_{n+1}}\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}\,\mathrm{d}t = \frac{h}{2}\sum_{i=1}^{2}w(i)\,\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}(\xi_{i})\nabla_{t}\boldsymbol{\phi}_{\mathcal{K}_{t}}^{T}(\xi_{i}) =: \mathbf{W} ]$$

其中 $[ h ]$ 为时间步长，$[ w(i) ]$ 为 Gauss 积分权系数（由 `gaussInt.m` 给出）。

---

#### 公式 (50)：子矩阵的分块符号约定

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 78–89 行）

代码中用显式索引提取 $\mathbf{K}_0$ 和 $\mathbf{K}_\varepsilon$ 的各分块：

```matlab
int_uu11 = int_uu(1:Nv,       1:Nv);
int_uu12 = int_uu(1:Nv,       Nv+1:2*Nv);
int_uu21 = int_uu(Nv+1:2*Nv, 1:Nv);
int_uu22 = int_uu(Nv+1:2*Nv, Nv+1:2*Nv);
```

**公式 (50)**

$$[ \mathbf{K}_{\mathrm{any}} = \begin{bmatrix}\mathbf{K}_{\mathrm{any}}^{I,I} & \mathbf{K}_{\mathrm{any}}^{I,II}\\\mathbf{K}_{\mathrm{any}}^{II,I} & \mathbf{K}_{\mathrm{any}}^{II,II}\end{bmatrix} ]$$

---

#### 公式 (51)：$\mathbf{K}_{vv}$ 的最终矩阵表达式

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 52–66 行）

```matlab
% K_{vv} = -ρ^{-1}(L⊗K_0 + θh Q⊗K_0)（对角块，x/y 分量）
Kpp_c = [Int*NN(1,1),  Int*NN(1,2);
         Int*NN(2,1),  Int*NN(2,2)] + ...
         theta*h*[Int*dNN(1,1), Int*dNN(1,2);
                  Int*dNN(2,1), Int*dNN(2,2)];
Kpp_c = blkdiag(Kpp_c, Kpp_c);   % 扩展到二维向量场（x/y 对角块相等）
```

（最终在 `AK` 中取 `-1/rho*Kpp_c`）

**公式 (51)**

$$[ \mathbf{K}_{\boldsymbol{vv}} = \rho^{-1}\begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_{0}^{I,I}&\\&\mathbf{L}\otimes\mathbf{K}_{0}^{II,II}\end{bmatrix} + \rho^{-1}\theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_{0}^{I,I}&\\&\mathbf{Q}\otimes\mathbf{K}_{0}^{II,II}\end{bmatrix} ]$$

---

#### 公式 (52)：$\mathbf{K}_{vu}$ 的矩阵表达式

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 56–65 行）

```matlab
% K_{vu} = R⊗K_0 + θh W⊗K_0
Kpu_c = [Int*NdN(1,1),  Int*NdN(1,2);
         Int*NdN(2,1),  Int*NdN(2,2)] + ...
    theta*h*[Int*dNdN(1,1), Int*dNdN(1,2);
             Int*dNdN(2,1), Int*dNdN(2,2)];
Kpu_c = blkdiag(Kpu_c, Kpu_c);
```

**公式 (52)**

$$[ \mathbf{K}_{\boldsymbol{vu}} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_{0}^{I,I}&\\&\mathbf{R}\otimes\mathbf{K}_{0}^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_{0}^{I,I}&\\&\mathbf{W}\otimes\mathbf{K}_{0}^{II,II}\end{bmatrix} ]$$

---

#### 公式 (53)：$\mathbf{K}_{uv}$ 的矩阵表达式

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 60–66 行）

```matlab
% K_{uv} = R⊗K_0 + θh W⊗K_0（与 K_{vu} 结构相同）
Kup_c = [Int*NdN(1,1),  Int*NdN(1,2);
         Int*NdN(2,1),  Int*NdN(2,2)] + ...
    theta*h*[Int*dNdN(1,1), Int*dNdN(1,2);
             Int*dNdN(2,1), Int*dNdN(2,2)];
Kup_c = blkdiag(Kup_c, Kup_c);
```

**公式 (53)**

$$[ \mathbf{K}_{\boldsymbol{uv}} = \begin{bmatrix}\mathbf{R}\otimes\mathbf{K}_{0}^{I,I}&\\&\mathbf{R}\otimes\mathbf{K}_{0}^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{W}\otimes\mathbf{K}_{0}^{I,I}&\\&\mathbf{W}\otimes\mathbf{K}_{0}^{II,II}\end{bmatrix} ]$$

---

#### 公式 (54)：$\mathbf{K}_{uu}$ 的矩阵表达式

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 82–89 行）

```matlab
% K_{uu} = L⊗K_ε + θh Q⊗K_ε（含所有分块组合）
Kuu_c = [NN(1,1)*int_uu11, NN(2,1)*int_uu11, NN(1,1)*int_uu12, NN(2,1)*int_uu12;
         NN(1,2)*int_uu11, NN(2,2)*int_uu11, NN(1,2)*int_uu12, NN(2,2)*int_uu12;
         NN(1,1)*int_uu21, NN(2,1)*int_uu21, NN(1,1)*int_uu22, NN(2,1)*int_uu22;
         NN(1,2)*int_uu21, NN(2,2)*int_uu21, NN(1,2)*int_uu22, NN(2,2)*int_uu22] + ...
    theta*h*[dNN(1,1)*int_uu11, dNN(1,2)*int_uu11, dNN(1,1)*int_uu12, dNN(1,2)*int_uu12;
             dNN(2,1)*int_uu11, dNN(2,2)*int_uu11, dNN(2,1)*int_uu12, dNN(2,2)*int_uu12;
             dNN(1,1)*int_uu21, dNN(1,2)*int_uu21, dNN(1,1)*int_uu22, dNN(1,2)*int_uu22;
             dNN(2,1)*int_uu21, dNN(2,2)*int_uu21, dNN(2,1)*int_uu22, dNN(2,2)*int_uu22];
```

（`NN` = $\mathbf{L}$，`dNN` = $\mathbf{Q}$，`int_uu` = $\mathbf{K}_\varepsilon$）

**公式 (54)**

$$[ \mathbf{K}_{\boldsymbol{uu}} = \begin{bmatrix}\mathbf{L}\otimes\mathbf{K}_{\varepsilon}^{I,I} & \mathbf{L}\otimes\mathbf{K}_{\varepsilon}^{I,II}\\\mathbf{L}\otimes\mathbf{K}_{\varepsilon}^{II,I} & \mathbf{L}\otimes\mathbf{K}_{\varepsilon}^{II,II}\end{bmatrix} + \theta h\begin{bmatrix}\mathbf{Q}\otimes\mathbf{K}_{\varepsilon}^{I,I} & \mathbf{Q}\otimes\mathbf{K}_{\varepsilon}^{I,II}\\\mathbf{Q}\otimes\mathbf{K}_{\varepsilon}^{II,I} & \mathbf{Q}\otimes\mathbf{K}_{\varepsilon}^{II,II}\end{bmatrix} ]$$

---

## 4. 空时动力学有限元法（FEM for Space–Time Dynamics）

---

#### 公式 (55)：FEM 质量矩阵 $\mathbf{K}^0$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 21–35 行，VEM 近似；FEM 版本结构相同，仅将 VEM 投影替换为 FEM 形函数 $\phi_\mathrm{FEM}$）

对于 FEM 四边形元，$\mathbf{K}^0 = \int_{\kappa_x} [\phi_\mathrm{FEM}][\phi_\mathrm{FEM}]^T d\Omega$ 通过 Gauss 积分直接计算：

```matlab
% 在 VEM 代码中，等价形式为：
% K^0 = Int = K_c^0 + K_s^0（VEM 版本）
% FEM 版本去掉稳定项，直接计算形函数矩阵的积分：
Int = zeros(Nv);
for iel = 1:NT
    vT   = nodeT(elemT(iel,:),:);
    area = 0.5*abs(det([[1;1;1],vT]));
    xy   = lambda*vT;
    for p = 1:size(xy,1)
        M   = polyBisis(xy(p,:), hK, centroid, 1, 2);
        Pim = M*Pis;
        Int = Int + area*weight(p)*(Pim'*Pim);
    end
end
```

**公式 (55)**

$$[ \mathbf{K}^{0} = \int_{\kappa_{x}}[\phi_{\mathrm{FEM}}][\phi_{\mathrm{FEM}}]^{T}\,\mathrm{d}\Omega ]$$

---

#### 公式 (56)：FEM 刚度矩阵 $\mathbf{K}^\varepsilon$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 68–78 行，VEM 版本；FEM 版本结构类似，直接用 $B$ 矩阵）

FEM 版本直接用应变-位移矩阵 $\mathbf{B}$：$\mathbf{K}^\varepsilon = \int B^T \hat{D} B d\Omega$，在 VEM 中对应：

```matlab
% VEM 等价实现：
dM1      = blkdiag(dM', dM');          % ∇M（VEM 多项式梯度）
G0       = Area * dM1'*A'*D*A*dM1;    % ∫B_vem^T D̃ B_vem dΩ
int_uu_c = Pis'*G0*Pis;               % K^ε = (Π^*)^T G0 Π^*（含投影）
```

**公式 (56)**

$$[ \mathbf{K}^{\varepsilon} = \int_{\kappa_{x}}\mathbf{B}^{T}\hat{\mathbb{D}}\mathbf{B}\,\mathrm{d}\Omega ]$$

---

#### 公式 (57)：FEM 应变-位移矩阵 $\mathbf{B}$

**最底层代码** — [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m)（第 68–73 行）+ [`myGradmc.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/myGradmc.m)（第 13–14 行）

VEM 版本中，$\mathbf{B}$ 的等价形式由 $\mathbf{A}\nabla\mathbf{M}\boldsymbol{\Pi}_\nabla^*$ 给出：

```matlab
% myGradmc.m：∇m_k（k=1）
dM = [0,0; 1./hK*[1,0]; 1./hK*[0,1]];  % [∇m_1; ∇m_2; ∇m_3]

% elemK.m：构造 B_vem = A [∇m_x 0; 0 ∇m_y; ∇m_y ∇m_x]（Voigt B 矩阵）
A1 = [1,0;0,0;0,1]; A2 = [0,0;0,1;1,0]; A = [A1,A2];
dM1 = blkdiag(dM', dM');               % = [∇m_k 0; 0 ∇m_k]
% B_vem = A * dM1（应变-位移矩阵的多项式空间版本）
G0 = Area * dM1'*A'*D*A*dM1;          % = |E| B_vem^T D̃ B_vem（单点近似）
```

**公式 (57)**

$$[ \mathbf{B} \coloneqq \begin{bmatrix}\dfrac{\partial\phi_{\mathrm{FEM}}^{T}}{\partial x} & \mathbf{0}^{T}\\\mathbf{0}^{T} & \dfrac{\partial\phi_{\mathrm{FEM}}^{T}}{\partial y}\\\dfrac{\partial\phi_{\mathrm{FEM}}^{T}}{\partial y} & \dfrac{\partial\phi_{\mathrm{FEM}}^{T}}{\partial x}\end{bmatrix} ]$$

---

## 附录：代码文件索引

| 代码文件 | 主要功能 | 对应公式 |
|:---|:---|:---|
| [`main.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/main.m) | 主程序入口，求解 $\mathbf{K}\mathbf{u}=\mathbf{f}$ | (2),(4),(9),(11) |
| [`globalK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/globalK.m) | 组装全局刚度矩阵 | (9),(43),(50) |
| [`elemK.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/elemK.m) | 单元刚度矩阵计算（核心） | (3),(6)-(8),(13),(14),(16)-(19),(27)-(54) |
| [`calculatePi.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/calculatePi.m) | 计算投影算子 $\Pi_\nabla^{k,E}$ | (20)-(25),(28),(31),(36) |
| [`myM.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/myM.m) | 计算缩放单项式 $\boldsymbol{m}_k$ | (24) |
| [`polyBisis.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/polyBisis.m) | 计算缩放单项式（积分点处） | (24),(25),(29) |
| [`myGradmc.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/myGradmc.m) | 计算 $\nabla\boldsymbol{m}_k$ | (28),(31),(37),(57) |
| [`auxgeometry.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/auxgeometry.m) | 计算单元几何量 $\|E\|,\,\boldsymbol{x}_E,\,h_E$ | 3.1节网格参数 |
| [`gaussInt.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/gaussInt.m) | Gauss 积分点和权系数（时间方向） | (46)-(49) |
| [`quadpts.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/quadpts.m) | 三角形 Gauss 积分点（空间域） | (29),(30) |
| [`fun.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/fun.m) | 形函数计算（时间/空间） | (39),(40),(46)-(49) |
| [`dfun.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/dfun.m) | 形函数导数（时间方向，$\nabla_t\Phi_{\kappa_t}$） | (40),(47),(49) |
| [`getForce.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/getForce.m) | 组装外力向量 | (5),(17),(19) |
| [`boudnaryCondition.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/boudnaryCondition.m) | 施加 Dirichlet 边界条件 | — |
| [`PolyMesh3Simple.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/PolyMesh3Simple.m) | 生成时空柱体网格 | (1),(38),(41) |
| [`auxstructure.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/auxstructure.m) | 生成网格拓扑辅助结构 | 3.1节 |
| [`findFace.m`](https://github.com/marksweli/VEM-spcae-time-dynamic/blob/main/findFace.m) | 查找载荷施加边界面 | (17),(19) |
